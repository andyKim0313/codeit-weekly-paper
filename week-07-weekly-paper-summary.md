# 위클리 페이퍼 #7

> **주제:** 이미지 전처리, 데이터 증강, 전이 학습  
> **작성자:** 이름을 입력하세요  
> **작성일:** 2026년 8월 10일

---

## 1. 리사이징과 정규화를 하는 이유

이미지는 촬영 장치와 저장 방식에 따라 크기, 비율과 픽셀값의 범위가 다를 수 있다. 모델에 입력하기 전 리사이징과 정규화를 적용하면 입력 형식을 통일하고 모델이 안정적으로 학습할 수 있다.

### 1.1 리사이징

리사이징(Resizing)은 이미지의 가로와 세로 크기를 모델이 요구하는 크기로 변경하는 작업이다.

#### 필요한 이유

1. **배치 구성을 위해서**  
   한 배치의 이미지들은 보통 하나의 텐서로 묶인다. 크기가 제각각이면 같은 텐서로 묶기 어렵기 때문에 일정한 크기로 맞춰야 한다.

2. **모델의 입력 규격을 맞추기 위해서**  
   사전 학습 모델은 보통 특정 입력 크기와 전처리 방식을 기대한다. 모델에 맞는 크기로 변환해야 기존 구조와 가중치를 올바르게 사용할 수 있다.

3. **연산량과 메모리 사용을 조절하기 위해서**  
   이미지가 클수록 처리해야 할 픽셀이 많아져 학습 시간과 메모리 사용량이 증가한다. 적절한 크기로 줄이면 학습 효율을 높일 수 있다.

#### 주의할 점

- 가로세로 비율을 무시하고 강제로 변환하면 객체의 모양이 왜곡될 수 있다.
- 비율을 유지한 Resize 뒤 Center Crop을 적용하거나, 남는 부분을 Padding으로 채울 수 있다.
- 지나치게 작게 줄이면 작은 객체와 세부적인 특징이 사라질 수 있다.
- 객체 탐지나 분할에서는 이미지와 함께 바운딩 박스와 마스크도 동일하게 변환해야 한다.

### 1.2 정규화

이미지 전처리에서 정규화(Normalization)는 픽셀값을 일정한 범위나 분포로 변환하는 것을 의미한다.

정규화라는 표현은 다음 두 작업을 넓게 포함해 사용되기도 한다.

| 방법 | 설명 |
|---|---|
| Rescaling | 픽셀값을 0~1처럼 작은 범위로 변환 |
| 채널별 표준화 | 각 RGB 채널에서 평균을 빼고 표준편차로 나눔 |

#### 필요한 이유

- 입력값의 규모를 일정하게 만들어 학습을 안정적으로 한다.
- 지나치게 큰 픽셀값 때문에 기울기 업데이트가 불안정해지는 것을 줄인다.
- RGB 채널 사이의 값 차이를 조정해 최적화를 돕는다.
- 전이 학습에서는 사전 학습 당시 사용한 입력 분포와 맞출 수 있다.

특히 사전 학습 모델을 사용할 때는 임의의 평균과 표준편차를 사용하기보다 해당 가중치에 지정된 공식 전처리 방법을 따라야 한다.

### PyTorch 전처리 예시

```python
from torchvision.transforms import v2
import torch

transform = v2.Compose([
    v2.Resize((224, 224)),
    v2.ToImage(),
    v2.ToDtype(torch.float32, scale=True),
    v2.Normalize(
        mean=[0.485, 0.456, 0.406],
        std=[0.229, 0.224, 0.225],
    ),
])
```

> **한 문장 정리:** 리사이징은 이미지 크기를 통일해 모델 입력과 배치 연산을 가능하게 하고, 정규화는 픽셀값의 규모와 분포를 맞춰 학습을 안정적으로 만든다.

---

## 2. 데이터 증강과 주요 이미지 증강 기법

### 데이터 증강이란?

데이터 증강(Data Augmentation)은 기존 학습 데이터에 정답의 의미를 유지하는 무작위 변환을 적용해 다양한 학습 예시를 만드는 방법이다.

원본 이미지를 실제로 여러 장 저장하지 않고 학습할 때마다 다른 변환을 적용할 수 있다. 모델은 특정 위치, 밝기나 촬영 조건을 외우는 대신 변형에도 유지되는 핵심 특징을 학습하게 된다.

### 사용하는 이유

- 제한된 학습 데이터의 다양성을 높인다.
- 학습 이미지를 그대로 외우는 과적합을 완화한다.
- 위치, 크기, 밝기 등의 변화에 강한 모델을 만든다.
- 실제 환경에서 발생할 수 있는 변화를 학습에 반영한다.

데이터 증강은 원본에 없던 독립적인 정보를 새로 만들어내는 것은 아니다. 따라서 증강만으로 부족한 데이터 수집을 완전히 대신할 수는 없다.

### 주요 증강 기법

| 구분 | 기법 | 설명 |
|---|---|---|
| 기하학적 변환 | 좌우·상하 반전 | 이미지를 수평 또는 수직으로 뒤집음 |
| 기하학적 변환 | 회전 | 일정 각도 범위에서 이미지를 회전 |
| 기하학적 변환 | 이동·확대·축소 | 객체의 위치와 크기를 변경 |
| 기하학적 변환 | Random Crop | 임의 영역을 잘라 모델 입력 크기로 변환 |
| 색상 변환 | 밝기·대비 조절 | 조명 변화와 촬영 환경을 반영 |
| 색상 변환 | 채도·색조 조절 | 색상 조건을 다양하게 만듦 |
| 품질 변화 | Blur·Noise | 흐림이나 센서 잡음에 대한 강건성 향상 |
| 가림 | Random Erasing | 임의 영역을 가려 일부 정보가 없어도 학습하게 함 |
| 이미지 혼합 | MixUp | 두 이미지와 라벨을 비율에 따라 섞음 |
| 이미지 혼합 | CutMix | 한 이미지의 일부 영역을 다른 이미지로 교체 |

### 적용할 때 주의할 점

#### 1) 정답의 의미를 유지해야 한다

숫자 `6`을 뒤집으면 `9`처럼 보일 수 있고, 글자나 교통 표지판은 반전 시 의미가 달라질 수 있다. 의료 영상에서는 좌우 방향 자체가 중요한 정보일 수 있다. 따라서 데이터 특성에 맞는 변환만 사용해야 한다.

#### 2) 학습 데이터에만 무작위 증강을 적용한다

검증·테스트 데이터에는 일반적으로 Resize, Center Crop과 정규화처럼 결과가 일정한 전처리만 사용한다. 검증 이미지까지 무작위로 바꾸면 모델 비교 기준이 흔들릴 수 있다.

#### 3) 변환 강도를 지나치게 높이지 않는다

회전, 색상 변화나 가림이 너무 강하면 실제 데이터와 동떨어진 이미지를 만들고 학습을 방해할 수 있다. 원본과 증강 이미지를 직접 확인하며 적절한 범위를 정해야 한다.

#### 4) 라벨 정보도 함께 변환한다

객체 탐지와 이미지 분할에서는 이미지뿐 아니라 바운딩 박스, 마스크와 키포인트도 같은 방식으로 변환해야 한다.

### PyTorch 증강 예시

```python
train_transform = v2.Compose([
    v2.RandomResizedCrop((224, 224)),
    v2.RandomHorizontalFlip(p=0.5),
    v2.ColorJitter(brightness=0.2, contrast=0.2),
    v2.ToImage(),
    v2.ToDtype(torch.float32, scale=True),
    v2.Normalize(
        mean=[0.485, 0.456, 0.406],
        std=[0.229, 0.224, 0.225],
    ),
])
```

> **한 문장 정리:** 데이터 증강은 정답의 의미를 유지하는 반전, 회전, 자르기와 색상 변화 등을 무작위로 적용해 학습 데이터의 다양성과 모델의 일반화 성능을 높이는 방법이다.

---

## 3. 전이 학습과 이미지 분류에서의 활용

### 전이 학습이란?

전이 학습(Transfer Learning)은 한 문제와 대규모 데이터에서 학습한 모델의 지식을 새로운 관련 문제에 재사용하는 방법이다.

이미지 모델의 앞부분은 선, 모서리, 질감과 형태처럼 여러 이미지 문제에 공통으로 필요한 특징을 학습한다. 따라서 이 특징 추출 능력을 처음부터 다시 학습하지 않고 새로운 분류 문제에 활용할 수 있다.

```text
대규모 데이터로 사전 학습한 모델
           ↓
공통 이미지 특징을 추출하는 Backbone 재사용
           ↓
새로운 문제에 맞는 분류기 연결
           ↓
새 데이터로 학습 및 Fine-tuning
```

### 장점

- 적은 데이터로도 비교적 좋은 성능을 얻을 수 있다.
- 무작위 초기화부터 학습하는 것보다 빠르게 수렴할 수 있다.
- 학습 시간과 계산 비용을 줄일 수 있다.
- 검증된 모델 구조와 특징 추출 능력을 활용할 수 있다.

### 이미지 분류 모델에 적용하는 과정

#### 1) 사전 학습 모델을 선택한다

ImageNet 같은 대규모 이미지 데이터로 학습된 ResNet, EfficientNet 등의 모델과 가중치를 불러온다. 새로운 데이터와 가능한 한 관련 있는 데이터로 학습된 모델이면 더 유리할 수 있다.

#### 2) 기존 분류기를 교체한다

사전 학습 모델의 마지막 분류층은 기존 클래스 수에 맞춰져 있다. 이 레이어를 제거하고 새로운 데이터의 클래스 수에 맞는 레이어로 교체한다.

예를 들어 1,000개 클래스를 분류하던 모델을 고양이와 강아지 분류에 사용한다면 마지막 출력 수를 2개로 바꾼다.

#### 3) 특징 추출기를 고정하고 새 분류층을 학습한다

먼저 Backbone의 가중치를 동결하고 새로 추가한 분류층만 학습한다. 이 방식을 **고정 특징 추출(Fixed Feature Extraction)**이라고 한다.

데이터가 적거나 새로운 데이터가 사전 학습 데이터와 비슷할 때 안전하고 빠른 시작점이 된다.

#### 4) 필요한 레이어를 미세 조정한다

분류층이 어느 정도 학습된 뒤 Backbone의 일부 또는 전체를 다시 학습 가능하게 만든다. 작은 학습률을 사용해 기존의 유용한 특징을 크게 훼손하지 않으면서 새로운 데이터에 맞게 조정한다. 이를 **Fine-tuning**이라고 한다.

#### 5) 검증 데이터로 범위를 결정한다

어디까지 동결을 해제할지, 학습률을 얼마나 작게 할지 검증 성능을 기준으로 선택한다. 데이터가 사전 학습 데이터와 많이 다르면 더 많은 레이어를 조정해야 할 수 있다.

### 두 가지 활용 방식 비교

| 구분 | 고정 특징 추출 | Fine-tuning |
|---|---|---|
| Backbone | 동결 | 일부 또는 전체 학습 |
| 학습 대상 | 새 분류층 중심 | 분류층과 해제된 기존 레이어 |
| 속도와 자원 | 빠르고 적게 사용 | 더 많은 시간과 메모리 필요 |
| 적합한 상황 | 데이터가 적고 기존 문제와 유사함 | 데이터가 충분하거나 새 문제에 맞춘 조정이 필요함 |
| 주의점 | 새로운 도메인에 충분히 적응하지 못할 수 있음 | 큰 학습률 사용 시 기존 지식이 손상될 수 있음 |

### 주의할 점

- 사전 학습 모델이 기대하는 이미지 크기와 정규화 방식을 사용해야 한다.
- 새 데이터가 기존 데이터와 지나치게 다르면 전이 효과가 작거나 성능이 나빠지는 음의 전이가 발생할 수 있다.
- Fine-tuning은 새 분류층이 어느 정도 학습된 후 작은 학습률로 진행하는 것이 안전하다.
- 클래스 불균형, 데이터 누수와 잘못된 검증 방식은 전이 학습으로 해결되지 않는다.

> **한 문장 정리:** 전이 학습은 대규모 데이터에서 학습한 이미지 특징 추출기를 재사용하고 새 분류층을 학습한 뒤, 필요하면 기존 레이어 일부를 작은 학습률로 미세 조정하는 방법이다.

---

## 4. 발표용 추가 정리: 전이 학습

### 쉽게 설명하는 비유

전이 학습은 **이미 사진을 많이 본 숙련된 사람에게 새로운 분류 기준만 가르치는 것**과 비슷하다.

고양이, 자동차와 나무 등 수많은 이미지를 본 사람은 선, 모서리, 질감과 형태를 이미 구분할 수 있다. 이 사람에게 불량 제품과 정상 제품을 구분하게 할 때 시각 자체를 처음부터 가르칠 필요는 없다. 기존 시각 능력은 유지하고 새로운 판정 기준만 학습시키면 된다.

### 핵심 4단계

```text
1. 사전 학습 모델 가져오기
2. 기존 마지막 분류층 교체하기
3. Backbone을 동결하고 새 분류층 학습하기
4. 필요하면 일부 레이어를 작은 학습률로 Fine-tuning하기
```

### 2분 발표 대본

> 전이 학습은 한 문제에서 학습한 모델의 지식을 새로운 관련 문제에 재사용하는 방법입니다.
>
> 이미지 분류 모델의 앞쪽 레이어는 선, 모서리, 색 변화와 질감처럼 다양한 이미지에 공통으로 나타나는 특징을 학습합니다. 뒤쪽 레이어로 갈수록 이 특징들을 조합해 특정 객체를 구분합니다. 따라서 대규모 이미지로 미리 학습한 모델은 새로운 문제에서도 좋은 특징 추출기로 사용할 수 있습니다.
>
> 적용 과정은 네 단계로 설명할 수 있습니다. 먼저 ImageNet 등으로 학습된 ResNet과 같은 모델을 가져옵니다. 다음으로 기존 마지막 분류층을 제거하고 새로운 클래스 수에 맞는 분류층으로 교체합니다. 처음에는 기존 Backbone의 가중치를 동결하고 새 분류층만 학습합니다. 이를 고정 특징 추출이라고 합니다.
>
> 이후 성능을 더 높일 필요가 있다면 Backbone의 위쪽 레이어 일부를 동결 해제하고 작은 학습률로 다시 학습합니다. 이것이 Fine-tuning입니다. 학습률을 작게 사용하는 이유는 기존 모델이 배운 유용한 특징을 한 번에 크게 바꾸지 않기 위해서입니다.
>
> 전이 학습은 데이터가 적을 때 특히 유용하며, 처음부터 모델을 학습하는 것보다 빠르고 적은 자원으로 좋은 성능을 얻을 수 있습니다. 다만 사전 학습 데이터와 새로운 데이터가 너무 다르면 전이 효과가 작을 수 있습니다.

### 예상 추가 질문과 답변

**Q. 처음부터 학습하지 않고 사전 학습 모델을 사용하는 이유는 무엇인가요?**  
대규모 데이터에서 이미 학습한 공통 이미지 특징을 활용하면 적은 데이터와 짧은 학습 시간으로도 좋은 시작점을 얻을 수 있기 때문이다.

**Q. 분류층만 학습하는 것과 Fine-tuning은 어떻게 다른가요?**  
분류층만 학습할 때는 Backbone을 고정하지만, Fine-tuning에서는 기존 레이어 일부도 새 데이터에 맞게 함께 조정한다.

**Q. Fine-tuning에서 작은 학습률을 사용하는 이유는 무엇인가요?**  
큰 업데이트로 사전 학습된 유용한 특징이 급격하게 손상되는 것을 막기 위해서이다.

**Q. 모든 문제에서 전이 학습이 효과적인가요?**  
아니다. 사전 학습 데이터와 새 데이터가 지나치게 다르거나 모델이 학습한 특징이 새 문제와 맞지 않으면 효과가 작거나 음의 전이가 생길 수 있다.

**Q. 언제 더 많은 레이어를 동결 해제해야 하나요?**  
새 데이터가 사전 학습 데이터와 다르고 데이터 수가 충분할수록 더 많은 레이어를 조정할 수 있다. 최종 범위는 검증 성능으로 결정한다.

---

## 5. 최종 정리

- 리사이징은 입력 크기를 통일하고 연산량을 조절한다.
- 정규화는 픽셀값의 규모와 분포를 맞춰 학습을 안정화한다.
- 데이터 증강은 실제로 가능한 변형을 적용해 다양성과 일반화 성능을 높인다.
- 증강 방식은 데이터의 의미와 라벨을 훼손하지 않아야 한다.
- 전이 학습은 사전 학습 모델의 특징 추출 능력을 새로운 문제에 재사용한다.
- 먼저 분류층을 학습하고 필요하면 일부 Backbone을 작은 학습률로 미세 조정한다.

---

## 6. 참고 자료

1. Torchvision, [Transforming Images, Videos, Boxes and More](https://docs.pytorch.org/vision/stable/transforms.html)
2. Torchvision, [Resize](https://docs.pytorch.org/vision/main/generated/torchvision.transforms.v2.Resize.html)
3. TensorFlow, [Data Augmentation](https://www.tensorflow.org/tutorials/images/data_augmentation)
4. PyTorch, [Transfer Learning for Computer Vision Tutorial](https://docs.pytorch.org/tutorials/beginner/transfer_learning_tutorial.html)
5. Keras, [Transfer Learning and Fine-tuning](https://keras.io/guides/transfer_learning/)

