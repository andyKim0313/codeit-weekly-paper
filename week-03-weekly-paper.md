# 3주차 위클리 페이퍼

> **주제:** 결정 트리, 부스팅, PCA와 요인 분석  
> **작성자:** 이름을 입력하세요  
> **작성일:** 2026년 7월 27일

## 목차

1. [결정 트리의 장점과 단점](#1-결정-트리의-장점과-단점)
2. [부스팅의 특징과 주요 부스팅 모델](#2-부스팅의-특징과-주요-부스팅-모델)
3. [주성분 분석과 요인 분석의 차이](#3-주성분-분석과-요인-분석의-차이)
4. [전체 내용 정리](#4-전체-내용-정리)
5. [참고 자료](#5-참고-자료)

---

## 1. 결정 트리의 장점과 단점

### 1.1 결정 트리란?

결정 트리(Decision Tree)는 데이터의 특성에 대한 질문을 순서대로 던지면서 데이터를 분할하고, 마지막 리프 노드에서 예측값을 출력하는 지도학습 모델이다. 분류와 회귀 문제에 모두 사용할 수 있다.

예를 들어 대출 승인 여부를 예측하는 모델은 다음과 같은 규칙을 학습할 수 있다.

```text
연 소득이 4,000만 원 이상인가?
├── 예: 기존 연체 기록이 있는가?
│   ├── 예: 대출 거절
│   └── 아니오: 대출 승인
└── 아니오: 부채 비율이 30% 이하인가?
    ├── 예: 대출 승인
    └── 아니오: 대출 거절
```

각 내부 노드는 하나의 분할 조건을, 가지는 조건의 결과를, 리프 노드는 최종 예측을 나타낸다. 이렇게 학습된 규칙을 `if-else` 문처럼 읽을 수 있다는 점이 결정 트리의 가장 큰 특징이다.

### 1.2 결정 트리는 어떻게 분할 기준을 선택하는가?

결정 트리는 현재 노드의 데이터를 더 순수한 자식 노드로 나눌 수 있는 특성과 기준값을 탐색한다. 분류에서는 지니 불순도(Gini impurity), 엔트로피(Entropy) 등을 많이 사용하고, 회귀에서는 평균제곱오차(MSE)나 절대오차 등을 사용할 수 있다.

분류 문제에서 지니 불순도는 다음과 같이 계산한다.

```math
Gini(t)=1-\sum_{k=1}^{K}p_{k}^{2}
```

- $p_k$: 노드 $t$에서 클래스 $k$가 차지하는 비율
- 한 클래스만 존재하면 지니 불순도는 0이다.
- 여러 클래스가 섞일수록 불순도가 커진다.

분할 전후의 불순도 감소량은 다음과 같이 표현할 수 있다.

```math
\Delta I = I(t) - \frac{n_L}{n_t}I(t_L) - \frac{n_R}{n_t}I(t_R)
```

결정 트리는 일반적으로 각 노드에서 $\Delta I$가 가장 커지는 분할을 선택한다. 중요한 점은 이 과정이 미래의 모든 분할을 한꺼번에 고려하는 것이 아니라 **현재 노드에서 가장 좋아 보이는 분할을 선택하는 탐욕적(greedy) 방식**이라는 것이다.

### 1.3 결정 트리의 장점

#### 1) 결과를 이해하고 설명하기 쉽다

트리를 시각화하면 어떤 특성과 기준을 거쳐 예측이 만들어졌는지 확인할 수 있다. 따라서 선형 모델의 계수나 신경망의 수많은 가중치보다 비전문가에게 설명하기 쉽다.

다만 트리가 지나치게 깊어지면 규칙의 수가 많아지므로, 결정 트리라고 해서 항상 해석하기 쉬운 것은 아니다. 해석 가능성은 트리의 깊이와 크기가 적절히 제한되었을 때 특히 높다.

#### 2) 데이터 전처리가 비교적 적다

결정 트리는 주로 `특성값 ≤ 기준값`과 같은 규칙을 사용한다. 특성의 값 자체보다 값의 순서와 분할 위치가 중요하기 때문에, 거리 기반 모델과 달리 표준화나 정규화가 일반적으로 필요하지 않다.

예를 들어 키를 미터 단위로 표현하든 센티미터 단위로 표현하든 값의 순서는 변하지 않으므로 대응되는 분할을 찾을 수 있다. 다만 결측값과 범주형 변수의 처리 방법은 사용하는 라이브러리와 알고리즘에 따라 다르므로 별도로 확인해야 한다.

#### 3) 비선형 관계를 표현할 수 있다

선형 회귀는 입력과 출력 사이의 관계를 하나의 선형식으로 표현하지만, 결정 트리는 데이터 공간을 여러 구역으로 나누어 각 구역마다 다른 예측을 할 수 있다. 따라서 별도의 다항식이나 상호작용 항을 직접 만들지 않아도 비선형 관계와 특성 간 상호작용을 학습할 수 있다.

#### 4) 분류와 회귀에 모두 사용할 수 있다

- 분류 트리: 리프 노드의 다수 클래스 또는 클래스 확률을 출력한다.
- 회귀 트리: 리프 노드에 속한 학습 데이터의 평균값 등을 출력한다.

동일한 트리 구조를 여러 종류의 예측 문제에 적용할 수 있다는 장점이 있다.

#### 5) 예측 속도가 빠르다

새로운 데이터는 루트 노드에서 시작해 조건에 따라 하나의 경로만 따라간다. 따라서 예측 비용은 전체 학습 데이터를 다시 비교하는 것이 아니라 트리의 깊이에 비례한다. 트리의 크기가 적절하다면 실시간 예측에도 활용할 수 있다.

#### 6) 특성 선택 효과가 있다

학습 과정에서 불순도 감소에 도움이 되는 특성만 분할에 사용된다. 유용하지 않은 특성은 트리에 선택되지 않을 수 있으므로 일정한 특성 선택 효과를 얻을 수 있다. 다만 불순도 기반 특성 중요도에는 편향이 생길 수 있으므로, 이를 곧바로 인과관계로 해석해서는 안 된다.

### 1.4 결정 트리의 단점

#### 1) 과적합하기 쉽다

트리의 깊이를 제한하지 않으면 각 리프에 매우 적은 학습 데이터만 남을 때까지 계속 분할할 수 있다. 이렇게 만들어진 트리는 학습 데이터의 잡음까지 규칙으로 외우게 되어 학습 성능은 높지만 새로운 데이터의 성능은 낮아질 수 있다.

다음과 같은 방법으로 과적합을 완화할 수 있다.

| 방법 | 역할 |
|---|---|
| `max_depth` 제한 | 트리가 지나치게 깊어지는 것을 방지한다. |
| `min_samples_split` 증가 | 적은 표본을 가진 노드의 추가 분할을 제한한다. |
| `min_samples_leaf` 증가 | 리프 노드가 최소한의 표본을 갖도록 한다. |
| `max_leaf_nodes` 제한 | 전체 리프 노드 수를 제한한다. |
| 가지치기(Pruning) | 효과가 작은 가지를 학습 후 제거한다. |
| 교차 검증 | 일반화 성능이 좋은 복잡도를 선택한다. |

#### 2) 데이터가 조금만 변해도 트리 구조가 크게 달라질 수 있다

상위 노드에서 선택한 분할은 아래의 모든 분할에 영향을 준다. 따라서 학습 데이터 일부가 추가되거나 제거되어 루트 근처의 분할이 바뀌면 전체 트리 구조가 달라질 수 있다. 이를 결정 트리의 **높은 분산(high variance)** 또는 불안정성이라고 한다.

랜덤 포레스트는 서로 다른 데이터와 특성을 사용해 여러 트리를 학습한 뒤 평균을 내어 이러한 분산을 줄인다. 부스팅 역시 단일 트리 여러 개를 결합하지만, 앞선 트리의 오류를 다음 트리가 순차적으로 보완한다는 차이가 있다.

#### 3) 전역 최적 트리를 보장하지 않는다

각 노드에서 가장 좋은 분할을 선택하는 탐욕적 알고리즘은 현재 단계의 최선만 고려한다. 초기에 조금 덜 좋아 보이는 분할이 이후 단계까지 고려했을 때 더 좋은 트리를 만들 수도 있지만, 일반적인 알고리즘은 모든 가능한 트리 구조를 탐색하지 않는다. 따라서 만들어진 트리가 가능한 모든 트리 중 전역적으로 가장 좋은 트리라고 보장할 수 없다.

#### 4) 축에 평행한 경계를 만들기 쉽다

일반적인 결정 트리는 한 번에 하나의 특성을 기준으로 분할한다. 따라서 대각선이나 원형에 가까운 결정 경계를 표현하려면 계단 모양의 분할이 많이 필요하다. 이 경우 트리가 불필요하게 깊어지고 일반화 성능이 나빠질 수 있다.

#### 5) 회귀에서 매끄러운 예측과 외삽에 약하다

회귀 트리는 각 리프에서 일정한 값을 출력하므로 예측 함수가 계단 모양이 된다. 또한 학습 데이터의 범위를 벗어난 값에 대해 선형 추세를 연장하기보다는 기존 리프의 값을 그대로 출력한다. 따라서 시간에 따른 증가 추세처럼 학습 범위 밖의 값을 예측해야 하는 문제에서는 한계가 있다.

#### 6) 클래스 불균형의 영향을 받을 수 있다

한 클래스의 데이터가 매우 많으면 다수 클래스를 중심으로 분할이 만들어질 수 있다. 이 경우 정확도는 높아 보여도 소수 클래스를 제대로 찾지 못할 수 있다. 클래스 가중치, 언더샘플링·오버샘플링, 적절한 평가지표 등을 함께 고려해야 한다.

### 1.5 결정 트리에 대한 정리

결정 트리는 전처리가 비교적 적고 예측 규칙을 설명하기 쉬우며 비선형 관계를 학습할 수 있다. 반면 깊은 트리는 과적합하기 쉽고, 데이터 변화에 민감하며, 탐욕적 분할로 인해 전역 최적해를 보장하지 않는다. 따라서 단독으로 사용할 때는 트리의 복잡도를 제한해야 하고, 더 안정적인 성능이 필요할 때는 랜덤 포레스트나 부스팅 같은 앙상블 방법을 고려할 수 있다.

---

## 2. 부스팅의 특징과 주요 부스팅 모델

### 2.1 부스팅이란?

부스팅(Boosting)은 여러 개의 약한 학습기(weak learner)를 **순차적으로 학습**하고 결합하여 하나의 강한 학습기를 만드는 앙상블 기법이다. 약한 학습기는 무작위 추측보다 조금 나은 정도의 단순한 모델을 의미하며, 얕은 결정 트리가 자주 사용된다.

부스팅의 일반적인 가법 모형은 다음과 같이 표현할 수 있다.

```math
F_M(x)=F_0(x)+\sum_{m=1}^{M}\eta\alpha_m h_m(x)
```

- $F_0(x)$: 초기 예측
- $h_m(x)$: $m$번째 약한 학습기
- $\alpha_m$: 해당 학습기의 기여도
- $\eta$: 학습률
- $M$: 약한 학습기의 수

첫 번째 모델이 틀린 부분을 확인하고 두 번째 모델이 그 부분을 더 잘 맞히도록 학습한다. 세 번째 모델은 앞선 두 모델을 합친 결과의 오류를 보완한다. 이 과정을 반복하면 단순한 모델 여러 개가 복잡한 함수를 표현할 수 있다.

### 2.2 부스팅의 핵심 특징

#### 1) 순차적으로 학습한다

배깅(Bagging)은 여러 모델을 서로 독립적으로 학습할 수 있지만, 부스팅의 다음 모델은 이전까지의 결과가 있어야 학습할 대상을 정할 수 있다. 따라서 트리 간 학습을 완전히 병렬화하기는 어렵다. 다만 XGBoost나 LightGBM 같은 라이브러리는 한 트리를 만드는 내부 연산을 병렬화하여 학습 속도를 높인다.

#### 2) 어려운 데이터나 현재의 오차에 집중한다

- AdaBoost는 잘못 분류된 샘플의 가중치를 높인다.
- Gradient Boosting은 현재 모델에서 계산한 손실 함수의 음의 기울기, 즉 의사 잔차(pseudo-residual)를 다음 모델이 학습한다.

구체적인 방법은 다르지만, 현재 앙상블이 부족한 부분에 다음 학습기를 집중시킨다는 공통점이 있다.

#### 3) 약한 학습기의 예측을 가중 결합한다

모든 모델을 동일하게 취급하는 것이 아니라 모델의 성능이나 학습률을 반영하여 결과를 더한다. 개별 트리는 얕고 단순하지만 많은 트리를 누적하면 복잡한 비선형 관계를 표현할 수 있다.

#### 4) 규제가 중요하다

반복 횟수와 트리의 복잡도가 지나치면 학습 데이터의 잡음까지 따라갈 수 있다. 다음 요소를 조절해야 한다.

- 학습률(`learning_rate`)
- 트리 수(`n_estimators`)
- 트리 깊이 또는 리프 수
- 행·열 샘플링 비율
- L1·L2 규제
- 조기 종료(early stopping)

일반적으로 학습률을 낮추면 한 트리가 미치는 영향이 줄어드는 대신 더 많은 트리가 필요하다.

### 2.3 배깅과 부스팅의 차이

| 구분 | 배깅 | 부스팅 |
|---|---|---|
| 학습 방식 | 여러 모델을 독립적으로 학습 | 이전 모델의 결과를 이용해 순차 학습 |
| 데이터 처리 | 부트스트랩 표본을 주로 사용 | 오분류 가중치 또는 잔차·기울기를 이용 |
| 결합 방법 | 평균 또는 다수결 | 가중합 |
| 주된 효과 | 분산 감소 | 편향을 줄이면서 강한 예측기 구성 |
| 병렬화 | 비교적 쉬움 | 트리 사이의 완전한 병렬화가 어려움 |
| 대표 모델 | Random Forest | AdaBoost, GBDT, XGBoost, LightGBM, CatBoost |

부스팅도 규제와 샘플링을 통해 분산을 줄일 수 있으므로, “배깅은 분산만 줄이고 부스팅은 편향만 줄인다”라고 절대적으로 구분하는 것은 정확하지 않다. 위 표는 두 방법의 주된 설계 목적과 경향을 비교한 것이다.

### 2.4 Gradient Boosting의 작동 원리

Gradient Boosting은 손실 함수를 줄이는 방향을 함수 공간에서 단계적으로 찾아가는 방법이다. $m$번째 단계에서 각 데이터의 의사 잔차는 다음과 같다.

```math
r_{im} = -\left[\frac{\partial L(y_i,F(x_i))}{\partial F(x_i)}\right]_{F=F_{m-1}}
```

새로운 약한 학습기 $h_m(x)$가 이 의사 잔차를 예측하도록 학습한 후 기존 모델에 더한다.

```math
F_m(x) = F_{m-1}(x)+\eta\gamma_mh_m(x)
```

평균제곱오차를 사용할 때 음의 기울기는 실제값과 예측값의 차이인 일반적인 잔차와 같다. 그러나 다른 손실 함수에서는 단순 잔차가 아니라 해당 손실 함수의 기울기를 학습한다. Gradient Boosting이 회귀와 분류를 비롯한 다양한 문제에 적용될 수 있는 이유가 여기에 있다.

### 2.5 XGBoost는 무엇을 개선했는가?

XGBoost는 Gradient Boosted Tree를 대규모 데이터에서도 효율적으로 학습할 수 있도록 만든 시스템이다. 목적 함수에 트리 복잡도에 대한 규제를 포함하고, 손실 함수의 1차 및 2차 미분값을 활용한다. 또한 희소 데이터 처리, 근사 분할 탐색, 캐시 접근, 병렬·분산 처리 등의 최적화를 제공한다.

즉, `Gradient Boosting`은 일반적인 학습 원리를 가리키고, `XGBoost`는 이 원리를 정규화와 시스템 최적화로 발전시킨 알고리즘 및 라이브러리라고 볼 수 있다.

### 2.6 Gradient Boosting과 XGBoost 이외의 부스팅 모델

> **분류상 주의할 점:** LightGBM과 CatBoost도 Gradient Boosting과 무관한 별개의 계열이 아니라, Gradient Boosted Decision Tree를 효율성이나 범주형 변수 처리 측면에서 개선한 모델이다. 반면 AdaBoost는 오분류 샘플의 가중치를 직접 조절한다는 점에서 작동 방식이 다르다.

#### 2.6.1 AdaBoost

AdaBoost(Adaptive Boosting)는 초기에 모든 학습 데이터에 같은 가중치를 부여한다. 모델이 샘플을 잘못 분류하면 해당 샘플의 가중치를 높이고, 올바르게 분류하면 가중치를 낮춘다. 다음 약한 학습기는 가중치가 커진 어려운 샘플에 더 집중한다. 최종 분류는 각 약한 학습기의 성능을 반영한 가중 투표로 결정한다.

**장점**

- 아이디어와 구현이 비교적 단순하다.
- 얕은 결정 트리처럼 단순한 학습기를 결합해 비선형 경계를 만들 수 있다.
- 조정할 핵심 파라미터가 비교적 적다.
- 여러 종류의 기본 학습기를 사용할 수 있다.

**단점**

- 틀린 샘플의 가중치를 계속 높이므로 이상치와 잘못된 정답 라벨에도 집중할 수 있다.
- 데이터에 잡음이 많으면 일반화 성능이 나빠질 수 있다.
- 순차 학습이므로 모델 간 완전한 병렬화가 어렵다.
- 복잡한 대규모 정형 데이터에서는 최신 GBDT 구현보다 성능이나 효율이 낮을 수 있다.

**적합한 상황**

- 데이터가 비교적 깨끗하고 규모가 크지 않은 분류 문제
- 단순한 기본 모델을 개선하고 싶은 경우
- 부스팅의 원리를 학습하거나 기준 모델을 만들고 싶은 경우

#### 2.6.2 LightGBM

LightGBM은 대규모·고차원 데이터에서 GBDT를 빠르고 메모리 효율적으로 학습하기 위해 개발되었다. 연속형 특성값을 구간으로 묶는 히스토그램 기반 분할을 사용하며, 논문에서는 다음 두 가지 기법을 핵심으로 제안했다.

- **GOSS(Gradient-based One-Side Sampling):** 기울기가 큰 샘플은 유지하고, 기울기가 작은 샘플의 일부만 사용하여 분할 이득을 효율적으로 추정한다.
- **EFB(Exclusive Feature Bundling):** 동시에 0이 아닌 경우가 드문 희소 특성을 묶어 실질적인 특성 수를 줄인다.

또한 일반적인 깊이 우선 성장 대신, 손실을 가장 크게 줄일 수 있는 리프를 먼저 분할하는 **leaf-wise 방식**을 사용한다.

**장점**

- 히스토그램을 사용해 학습 속도가 빠르고 메모리 사용량이 적다.
- 대규모 데이터와 특성이 많은 데이터에 적합하다.
- 희소 데이터, 병렬·분산 학습 및 GPU 학습을 지원한다.
- 같은 리프 수라면 손실을 크게 줄이는 분할에 집중할 수 있다.
- 범주형 특성을 위한 분할도 지원한다.

**단점**

- leaf-wise 성장은 특정 가지를 빠르게 깊게 만들 수 있어 작은 데이터에서 과적합하기 쉽다.
- `num_leaves`, `min_data_in_leaf`, `max_depth` 등의 파라미터를 데이터 규모에 맞게 조정해야 한다.
- 연속형 값을 구간화하므로 구간 수에 따라 속도와 정밀도 사이의 절충이 발생한다.
- 범주형 값은 라이브러리가 요구하는 형식에 맞게 지정해야 하며, 무조건 전처리가 필요 없는 것은 아니다.

**적합한 상황**

- 행과 열의 수가 많은 정형 데이터
- 학습 속도와 메모리 효율이 중요한 경우
- 희소한 특성이 많은 데이터

#### 2.6.3 CatBoost

CatBoost는 범주형 특성을 효과적으로 처리하기 위해 개발된 GBDT 모델이다. 단순한 타깃 인코딩은 한 샘플의 정답이 그 샘플의 인코딩 값에 반영되어 타깃 누수가 발생할 수 있다. CatBoost는 데이터 순서를 이용해 현재 샘플보다 앞선 샘플의 정보만으로 통계량을 계산하는 방식을 사용한다.

핵심 아이디어는 다음과 같다.

- **Ordered boosting:** 순열을 이용하여 현재 샘플의 학습에 그 샘플 자신의 정답 정보가 부적절하게 사용되는 편향을 줄인다.
- **Ordered categorical statistics:** 범주형 특성을 처리할 때 타깃 누수를 줄이는 순서 기반 통계량을 만든다.
- **대칭 트리(oblivious tree):** 같은 깊이의 노드에서 같은 분할 조건을 사용하는 구조로 빠른 추론과 규칙적인 모델 구조를 얻는다.

**장점**

- 범주형 특성을 원-핫 인코딩으로 모두 펼치지 않고 직접 처리할 수 있다.
- 범주형 변수가 많거나 범주 수가 많은 데이터에서 유용하다.
- 순서 기반 계산으로 타깃 누수와 prediction shift 문제를 줄이도록 설계되었다.
- 기본 설정만으로도 비교적 안정적인 성능을 내는 경우가 많다.

**단점**

- 순열과 순서 기반 통계량을 계산하므로 문제에 따라 추가적인 시간과 메모리가 필요할 수 있다.
- 범주형 변수가 거의 없는 순수 수치형 대규모 데이터에서는 LightGBM 등의 속도상 이점이 더 클 수 있다.
- 모델의 파라미터와 범주형 변수 처리 원리를 이해하지 않고 사용하면 데이터 누수 여부를 잘못 판단할 수 있다.
- 단일 결정 트리보다 모델 전체를 직접 해석하기 어렵다.

**적합한 상황**

- 고객 등급, 상품 종류, 지역처럼 범주형 특성이 많은 정형 데이터
- 범주 수가 많아 원-핫 인코딩의 차원이 크게 증가하는 경우
- 범주형 변수 전처리 부담과 타깃 누수 위험을 줄이고 싶은 경우

#### 2.6.4 NGBoost

일반적인 회귀 모델은 하나의 점 예측값을 출력한다. NGBoost(Natural Gradient Boosting)는 예측값 하나가 아니라 정규분포의 평균과 분산처럼 **조건부 확률분포의 파라미터**를 학습한다. 이를 통해 예측값뿐 아니라 예측 불확실성도 얻을 수 있다.

예를 들어 배송 시간을 단순히 `30분`이라고 예측하는 대신, 평균이 30분이고 표준편차가 5분인 확률분포를 예측할 수 있다. NGBoost는 여러 분포 파라미터를 함께 안정적으로 학습하기 위해 자연 기울기(Natural Gradient)를 사용한다.

**장점**

- 점 예측과 함께 예측 분포 및 불확실성을 제공한다.
- 기본 학습기, 확률분포, 평가 규칙을 조합할 수 있는 모듈형 구조이다.
- 의료, 날씨, 수요 예측처럼 불확실성 자체가 중요한 문제에 유용하다.
- 예측 구간이나 특정 임계값을 넘을 확률 등을 계산할 수 있다.

**단점**

- 선택한 확률분포가 실제 데이터와 맞지 않으면 불확실성 추정이 부정확할 수 있다.
- 여러 분포 파라미터를 학습하므로 일반적인 점 예측 모델보다 계산이 복잡하다.
- 점 예측 성능만 필요한 문제에서는 추가 복잡도의 이점이 작을 수 있다.
- 확률 예측은 평균 오차뿐 아니라 calibration과 확률적 평가지표로 별도 검증해야 한다.

**적합한 상황**

- 예측 구간과 위험 수준이 필요한 문제
- 의료 예후, 날씨, 수요량, 배송 시간처럼 결과의 불확실성이 중요한 경우
- 하나의 값보다 결과의 조건부 분포를 알고 싶은 경우

### 2.7 부스팅 모델 비교

| 모델 | 오류를 보완하는 핵심 방법 | 강점 | 주의점 | 추천 상황 |
|---|---|---|---|---|
| AdaBoost | 오분류 샘플의 가중치를 증가 | 단순하고 이해하기 쉬움 | 이상치와 라벨 잡음에 민감 | 비교적 깨끗한 소·중규모 데이터 |
| Gradient Boosting | 손실 함수의 음의 기울기를 학습 | 다양한 손실 함수 적용 가능 | 학습이 순차적이고 튜닝 필요 | 일반적인 정형 데이터 |
| XGBoost | 2차 미분, 규제, 희소·병렬 최적화 | 정확도와 확장성의 균형 | 파라미터가 많음 | 다양한 정형 데이터와 강한 기준 모델 |
| LightGBM | 히스토그램, GOSS, EFB, leaf-wise 성장 | 빠른 학습과 낮은 메모리 사용 | 작은 데이터에서 과적합 가능 | 대규모·고차원 데이터 |
| CatBoost | Ordered boosting과 범주형 통계 | 범주형 특성 처리에 강점 | 추가 계산 비용이 생길 수 있음 | 범주형 특성이 많은 데이터 |
| NGBoost | 분포 파라미터에 자연 기울기 적용 | 예측 불확실성 제공 | 분포 가정과 calibration 검증 필요 | 확률적 예측이 필요한 문제 |

### 2.8 부스팅에 대한 정리

부스팅의 본질은 단순히 트리를 많이 만드는 것이 아니라, **현재 모델이 부족한 부분을 다음 모델이 순차적으로 학습하도록 만드는 것**이다. AdaBoost는 틀린 샘플의 가중치를 높이고, Gradient Boosting 계열은 손실 함수의 기울기를 학습한다.

각 모델은 모든 상황에서 우수한 것이 아니라 데이터와 목적에 따라 선택해야 한다.

- 이해하기 쉬운 기본 부스팅이 필요하면 AdaBoost
- 일반적인 강력한 기준 모델이 필요하면 XGBoost
- 대규모 데이터와 속도가 중요하면 LightGBM
- 범주형 특성이 많으면 CatBoost
- 예측 불확실성이 필요하면 NGBoost

---

## 3. 주성분 분석과 요인 분석의 차이

### 3.1 두 방법이 비슷해 보이는 이유

주성분 분석(Principal Component Analysis, PCA)과 요인 분석(Factor Analysis, FA)은 모두 많은 관측변수를 더 적은 수의 새로운 변수로 표현할 수 있다. 두 방법 모두 로딩(loading)이라는 값을 통해 원래 변수와 새로운 차원의 관계를 살펴보기 때문에 결과표도 비슷해 보인다.

그러나 두 방법의 목적과 통계적 가정은 다르다.

- PCA의 핵심 질문: **원래 데이터의 정보를 최대한 보존하면서 더 적은 차원으로 압축하려면 어떻게 해야 하는가?**
- FA의 핵심 질문: **관측변수 사이의 상관관계를 만들어내는 보이지 않는 공통 요인은 무엇인가?**

### 3.2 주성분 분석(PCA)

PCA는 원래 변수들의 선형결합으로 새로운 축인 주성분을 만든다. 첫 번째 주성분은 데이터의 분산을 가장 크게 설명하는 방향이고, 두 번째 주성분은 첫 번째 주성분과 직교하면서 남은 분산을 가장 크게 설명하는 방향이다.

첫 번째 주성분의 방향 $w_1$은 다음과 같이 표현할 수 있다.

```math
w_1 = \underset{\lVert w\rVert=1}{\operatorname{argmax}} \operatorname{Var}(Xw)
```

두 번째 이후의 주성분은 앞선 주성분과 직교한다는 조건을 추가한다.

예를 들어 키, 몸무게, 허리둘레, 팔 길이처럼 서로 상관된 신체 측정값이 있다면 첫 번째 주성분은 전반적인 `신체 크기`와 비슷한 방향이 될 수 있다. 하지만 이 이름은 분석자가 결과를 보고 붙인 해석일 뿐, PCA 자체가 `신체 크기`라는 잠재 개념을 가정한 것은 아니다.

#### PCA의 주요 목적

- 차원 축소와 데이터 압축
- 서로 상관된 특성의 중복 제거
- 고차원 데이터의 시각화
- 다중공선성 완화
- 다른 머신러닝 모델에 입력할 새로운 특성 생성

#### PCA의 특징

- 관측된 변수의 **전체 분산**을 최대한 보존하려 한다.
- 주성분은 원래 변수의 결정적인 선형결합이다.
- 표준적인 PCA의 주성분들은 서로 직교한다.
- 전통적인 PCA는 잠재변수 생성 모형이나 변수별 측정오차를 필수로 가정하지 않는다.
- 큰 분산이 반드시 중요한 의미를 뜻하는 것은 아니다.

### 3.3 요인 분석(FA)

요인 분석은 여러 관측변수의 상관관계가 소수의 관측되지 않은 잠재 요인에 의해 만들어졌다고 가정한다. 관측변수 $x$를 다음과 같은 생성 모형으로 표현할 수 있다.

```math
x=\mu+\Lambda f+\epsilon
```

- $\mu$: 관측변수의 평균
- $f$: 관측되지 않은 공통 요인
- $\Lambda$: 요인 적재량 행렬
- $\epsilon$: 각 변수의 고유 요인 및 측정오차

공분산 구조는 다음과 같이 분해된다.

```math
\operatorname{Cov}(x)=\Lambda\Phi\Lambda^{T}+\Psi
```

- $\Lambda\Phi\Lambda^T$: 여러 관측변수가 공유하는 공통분산
- $\Psi$: 각 변수에만 존재하는 고유분산과 오차분산
- $\Phi$: 요인 사이의 공분산 행렬

예를 들어 설문 문항에 다음과 같은 질문이 있다고 가정한다.

- 이 서비스는 사용하기 쉽다.
- 원하는 기능을 쉽게 찾을 수 있다.
- 사용법을 빠르게 배울 수 있다.
- 이 서비스를 다른 사람에게 추천하고 싶다.
- 다음에도 이 서비스를 이용하고 싶다.
- 전반적으로 만족한다.

앞의 세 문항은 `사용 편의성`, 뒤의 세 문항은 `만족도`라는 잠재 요인의 영향을 받을 수 있다. 요인 분석의 목적은 이처럼 직접 관측할 수 없는 공통 개념과 각 문항의 관계를 설명하는 것이다.

#### FA의 주요 목적

- 관측변수 사이의 상관관계를 설명하는 잠재 구조 발견
- 설문 문항이 어떤 구성개념을 측정하는지 탐색
- 유사한 변수들을 공통 요인으로 묶어 해석
- 측정도구의 구성 타당도 검토

#### FA의 특징

- 전체 분산이 아니라 관측변수들이 공유하는 **공통분산**에 초점을 둔다.
- 변수별 고유분산과 오차분산을 모형에 포함한다.
- 요인은 관측변수를 만들어낸다고 가정하는 잠재변수이다.
- 요인 자체는 직접 관측되지 않으므로 요인 점수는 추정값이다.
- 해석을 쉽게 만들기 위해 Varimax 같은 직교 회전이나 oblique 회전을 사용할 수 있다.
- oblique 회전을 사용하면 요인들이 서로 상관될 수 있다.

### 3.4 PCA와 FA의 핵심 비교

| 비교 기준 | PCA | FA |
|---|---|---|
| 주된 목적 | 정보 보존과 차원 축소 | 잠재 요인과 공분산 구조 설명 |
| 기본 관점 | 관측변수를 새로운 축으로 변환 | 잠재 요인이 관측변수를 생성한다고 가정 |
| 사용하는 분산 | 전체 분산 | 공통분산 |
| 고유·오차분산 | 별도로 분리하지 않음 | 변수별 고유·오차분산을 명시적으로 포함 |
| 결과 변수 | 주성분 | 잠재 요인 |
| 새로운 차원의 값 | 관측변수의 선형결합으로 계산 | 관측되지 않아 요인 점수를 추정 |
| 직교성 | 표준 PCA의 주성분은 서로 직교 | 회전 방법에 따라 직교하거나 상관 가능 |
| 회전 | 기본 목적에 필수적이지 않음 | 해석 가능성을 높이기 위해 자주 사용 |
| 대표 활용 | 압축, 시각화, 모델 전처리 | 심리·교육·마케팅 설문, 잠재 구성개념 탐색 |

### 3.5 같은 데이터에 적용해도 결과가 다른 이유

PCA는 원래 데이터의 전체 분산을 많이 설명하는 방향을 우선한다. 어떤 변수에 측정오차나 그 변수만의 고유한 변동이 크다면, 그 변동도 PCA가 보존하려는 전체 분산에 포함된다.

반면 FA는 변수들이 함께 움직이는 부분을 공통 요인으로 설명하고, 특정 변수에만 존재하는 변동은 고유분산으로 분리하려 한다. 따라서 같은 상관행렬을 사용해도 PCA의 성분 적재량과 FA의 요인 적재량이 달라질 수 있다.

또한 FA에서는 요인 축을 회전해도 모형이 설명하는 공통 구조를 유지하면서 로딩의 모양을 다르게 표현할 수 있다. 이 때문에 요인은 회전 방법에 따라 해석이 달라질 수 있다. PCA의 주성분은 설명분산이 큰 순서로 정해지는 반면, FA는 단순하고 의미 있는 요인 구조를 찾는 해석 과정이 더 중요하다.

### 3.6 언제 어떤 방법을 사용해야 하는가?

#### PCA를 선택할 상황

- 수백 개의 센서 특성을 몇 개의 변수로 압축하고 싶다.
- 이미지나 유전자 데이터의 차원을 줄이고 싶다.
- 서로 강하게 상관된 변수 때문에 생기는 다중공선성을 완화하고 싶다.
- 20차원 데이터를 2차원으로 투영해 시각화하고 싶다.
- 잠재 개념의 의미보다 예측 모델의 효율이 더 중요하다.

#### FA를 선택할 상황

- 여러 설문 문항이 어떤 잠재 개념으로 묶이는지 알고 싶다.
- 고객 만족도를 `가격 만족`, `서비스 만족`, `사용 편의성` 등의 요인으로 설명하고 싶다.
- 관측값에 측정오차가 있다고 보고 공통 구조와 고유한 변동을 분리하고 싶다.
- 이론적으로 예상한 구성개념과 문항의 관계를 검토하고 싶다.

### 3.7 분석할 때 주의할 점

#### 1) 스케일을 확인해야 한다

변수의 단위가 크게 다르면 분산이 큰 변수가 PCA 결과를 지배할 수 있다. 예를 들어 연봉과 만족도 점수를 함께 분석하면 단위가 큰 연봉의 영향이 과도해질 수 있다. 이때는 변수를 표준화한 뒤 상관행렬을 기준으로 분석하는 방법을 고려해야 한다.

scikit-learn의 `PCA`는 기본적으로 각 특성의 평균을 빼는 중심화는 수행하지만, 분산을 1로 만드는 표준화까지 자동으로 수행하지는 않는다.

#### 2) 차원의 개수는 목적과 검증을 함께 고려해야 한다

PCA에서는 누적 설명분산 비율, scree plot, downstream 모델의 검증 성능 등을 사용할 수 있다. FA에서는 scree plot, parallel analysis, 적합도, 이론적 해석 가능성 등을 함께 살펴볼 수 있다. 단순히 고유값이 1보다 큰 요인만 남기는 규칙 하나에만 의존하는 것은 바람직하지 않다.

#### 3) 로딩이 비슷해 보여도 의미는 다르다

PCA 로딩은 원래 변수와 주성분의 관계를 나타내며, 정보 압축을 위한 변환의 일부이다. FA 로딩은 잠재 요인과 관측변수의 관계를 나타내는 모형 파라미터이다. 숫자의 형태가 비슷하더라도 같은 의미로 해석하면 안 된다.

### 3.8 PCA와 FA에 대한 정리

PCA와 FA는 모두 데이터의 차원을 줄일 수 있지만 출발점이 다르다. PCA는 원래 데이터의 전체 분산을 최대한 보존하는 새로운 축을 찾는 **변환·압축 기법**이다. FA는 관측변수의 상관관계를 설명하는 잠재 요인과 변수별 오차를 가정하는 **통계적 잠재변수 모형**이다.

따라서 데이터 압축과 예측 전처리가 목적이라면 PCA가 더 자연스럽고, 설문 문항 뒤에 있는 잠재 개념을 발견하고 해석하는 것이 목적이라면 FA가 더 적합하다.

---

## 4. 전체 내용 정리

이번 주제의 세 개념은 서로 연결되어 있다.

첫째, 결정 트리는 해석하기 쉽고 전처리가 적으며 비선형 관계를 학습할 수 있지만, 과적합과 불안정성이라는 약점이 있다. 트리의 깊이를 제한하거나 여러 트리를 결합하면 이러한 문제를 완화할 수 있다.

둘째, 부스팅은 여러 약한 학습기를 순차적으로 학습하여 현재 모델의 오류를 보완한다. AdaBoost는 오분류 샘플의 가중치에 집중하고, Gradient Boosting 계열은 손실 함수의 기울기를 학습한다. LightGBM은 대규모 데이터의 효율성, CatBoost는 범주형 변수, NGBoost는 예측 불확실성이라는 서로 다른 문제에 초점을 맞춘다.

셋째, PCA와 FA는 모두 적은 수의 차원으로 데이터를 표현하지만 목적이 다르다. PCA는 전체 분산을 보존하는 데이터 압축 기법이고, FA는 공통분산을 설명하는 잠재 요인 모형이다. 따라서 두 방법은 결과의 모양이 비슷하더라도 서로 바꾸어 사용할 수 있는 동일한 기법이 아니다.

### 기술 면접식 한 문장 답변

- **결정 트리의 장단점은 무엇인가?**  
  결정 트리는 전처리가 적고 규칙을 시각적으로 설명하기 쉬우며 비선형 관계를 학습할 수 있지만, 과적합과 데이터 변화에 민감한 높은 분산, 탐욕적 분할의 한계를 가진다.

- **부스팅은 어떤 앙상블인가?**  
  부스팅은 약한 학습기를 순차적으로 추가하면서 이전까지의 오분류나 손실의 기울기를 보완하고, 이들의 예측을 가중 결합해 강한 학습기를 만드는 앙상블 방법이다.

- **PCA와 FA의 가장 중요한 차이는 무엇인가?**  
  PCA는 전체 분산을 최대한 보존하는 관측변수의 선형결합을 찾는 반면, FA는 관측변수 사이의 공통분산을 설명하는 잠재 요인과 변수별 오차를 모형화한다.

---

## 5. 참고 자료

1. scikit-learn, [Decision Trees User Guide](https://scikit-learn.org/stable/modules/tree.html)
2. scikit-learn, [DecisionTreeClassifier API](https://scikit-learn.org/stable/modules/generated/sklearn.tree.DecisionTreeClassifier.html)
3. Jerome H. Friedman, [Greedy Function Approximation: A Gradient Boosting Machine](https://projecteuclid.org/download/pdf_1/euclid.aos/1013203451), *The Annals of Statistics*, 2001.
4. Yoav Freund and Robert E. Schapire, [A Decision-Theoretic Generalization of On-Line Learning and an Application to Boosting](https://doi.org/10.1006/jcss.1997.1504), *Journal of Computer and System Sciences*, 1997.
5. scikit-learn, [Ensembles: Gradient Boosting and AdaBoost](https://scikit-learn.org/stable/modules/ensemble.html)
6. Tianqi Chen and Carlos Guestrin, [XGBoost: A Scalable Tree Boosting System](https://arxiv.org/abs/1603.02754), KDD, 2016.
7. Guolin Ke et al., [LightGBM: A Highly Efficient Gradient Boosting Decision Tree](https://papers.nips.cc/paper/2017/hash/6449f44a102fde848669bdd9eb6b76fa-Abstract.html), NeurIPS, 2017.
8. LightGBM, [Features](https://lightgbm.readthedocs.io/en/stable/Features.html)
9. Liudmila Prokhorenkova et al., [CatBoost: Unbiased Boosting with Categorical Features](https://proceedings.neurips.cc/paper/2018/hash/14491b756b3a51daac41c24863285549-Abstract.html), NeurIPS, 2018.
10. Tony Duan et al., [NGBoost: Natural Gradient Boosting for Probabilistic Prediction](https://proceedings.mlr.press/v119/duan20a.html), ICML, 2020.
11. scikit-learn, [Decomposing Signals in Components](https://scikit-learn.org/stable/modules/decomposition.html)
12. scikit-learn, [FactorAnalysis API](https://scikit-learn.org/stable/modules/generated/sklearn.decomposition.FactorAnalysis.html)
