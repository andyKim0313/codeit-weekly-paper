# 2주차 위클리 페이퍼

> **주제:** 선형대수학, EDA, 데이터 전처리, 학습 방법과 모델 평가  
> **작성자:** 이름을 입력하세요  
> **작성일:** 2026년 7월 27일

---

## 1. 선형대수학이란 무엇이며, 왜 머신러닝에 필요한가?

### 선형대수학이란?

선형대수학(Linear Algebra)은 벡터, 행렬, 텐서와 이들 사이의 연산 및 선형 변환을 연구하는 수학 분야이다.

- **스칼라:** 하나의 숫자
- **벡터:** 여러 숫자를 한 방향으로 배열한 것
- **행렬:** 숫자를 행과 열로 배열한 것
- **텐서:** 벡터와 행렬을 더 높은 차원으로 일반화한 것

머신러닝에서는 하나의 데이터를 벡터로, 여러 데이터의 모음을 행렬로 표현할 수 있다. 예를 들어 학생 100명의 키, 몸무게, 공부 시간을 가진 데이터는 `100 × 3` 크기의 행렬로 나타낼 수 있다.

### 머신러닝에 필요한 이유

| 활용 영역 | 선형대수학의 역할 |
|---|---|
| 데이터 표현 | 여러 샘플과 특성을 벡터와 행렬로 표현한다. |
| 모델의 예측 | 입력 행렬과 가중치 벡터의 곱으로 예측값을 계산한다. |
| 유사도 계산 | 내적과 거리로 데이터 사이의 유사성을 측정한다. |
| 차원 축소 | PCA에서 고유값과 고유벡터를 이용한다. |
| 딥러닝 | 신경망의 순전파와 역전파를 행렬·텐서 연산으로 수행한다. |
| 연산 효율 | GPU가 대규모 행렬 연산을 병렬로 빠르게 처리한다. |

선형 회귀, PCA, 추천 시스템, 이미지 처리와 신경망 등 많은 알고리즘이 선형대수학으로 표현된다. 따라서 선형대수학을 이해하면 모델이 데이터를 어떻게 변환하고 학습하는지 파악하기 쉬워진다.

> **한 문장 정리:** 선형대수학은 벡터와 행렬 및 그 변환을 연구하는 학문이며, 머신러닝의 데이터·가중치·예측 과정을 수학적으로 표현하고 효율적으로 계산하기 위해 필요하다.

---

## 2. EDA란 무엇인가?

EDA(Exploratory Data Analysis)는 본격적인 모델링 전에 통계값과 시각화를 이용해 데이터의 구조, 품질, 분포와 변수 간 관계를 탐색하는 과정이다. 우리말로는 **탐색적 데이터 분석**이라고 한다.

EDA는 정해진 그래프 몇 개를 그리는 작업이라기보다, 데이터가 가진 특징과 문제를 열린 관점에서 발견하는 분석 방식에 가깝다.

### EDA의 주요 목적

- 데이터의 크기, 변수와 자료형 확인
- 평균, 중앙값, 표준편차 등 요약 통계 확인
- 결측값, 중복값과 이상치 발견
- 변수별 분포와 범주의 비율 파악
- 변수 사이의 관계와 상관관계 탐색
- 데이터 수집 및 입력 오류 발견
- 모델링에 필요한 가설과 전처리 방향 설정

### 대표적인 분석 방법

| 구분 | 예시 |
|---|---|
| 데이터 구조 | 행·열의 수, 자료형, 고유값 개수 확인 |
| 요약 통계 | 평균, 중앙값, 최솟값, 최댓값, 사분위수 |
| 단변량 분석 | 히스토그램, 막대그래프, 박스플롯 |
| 변수 관계 | 산점도, 상관행렬, 그룹별 통계 |
| 데이터 품질 | 결측률, 중복 행, 비정상 범위 확인 |

EDA에서 발견한 패턴은 확정적인 인과관계가 아니라 추가로 검증할 가설일 수 있다. 또한 전체 데이터를 먼저 살펴본 결과가 검증·테스트 데이터의 정보 누수로 이어지지 않도록 주의해야 한다.

> **한 문장 정리:** EDA는 모델링 전에 통계와 시각화를 통해 데이터의 구조, 분포, 관계와 품질 문제를 발견하고 이후 분석 방향을 정하는 과정이다.

---

## 3. 결측값, 중복값, 이상치 처리

데이터 전처리는 모델이 학습할 수 있는 신뢰도 높은 데이터를 만드는 과정이다. 값을 무조건 삭제하기보다, 문제가 발생한 원인과 데이터의 의미를 먼저 확인해야 한다.

### 3.1 결측값 처리

결측값(Missing Value)은 값이 기록되지 않았거나 알 수 없는 상태를 의미하며 보통 `NaN`, 빈 문자열, `NULL` 등으로 표현된다.

#### 처리 방법

| 방법 | 설명 | 주의점 |
|---|---|---|
| 행 삭제 | 결측값이 있는 행을 제거 | 데이터가 적거나 결측이 많으면 정보 손실이 큼 |
| 열 삭제 | 결측률이 매우 높거나 불필요한 열 제거 | 중요한 변수인지 먼저 확인해야 함 |
| 단순 대체 | 평균, 중앙값, 최빈값 또는 고정값으로 대체 | 분산과 변수 관계가 왜곡될 수 있음 |
| 모델 기반 대체 | 다른 변수를 이용해 결측값 추정 | 구현이 복잡하고 추정 오차가 생김 |
| 결측 여부 표시 | 결측 여부를 새로운 이진 특성으로 추가 | 결측 자체가 의미 있는 정보일 때 유용 |

이상치의 영향을 덜 받으려면 수치형 데이터에 평균 대신 중앙값을 사용할 수 있다. 범주형 데이터에는 최빈값이나 `Unknown` 같은 별도 범주를 사용할 수 있다.

결측값 대체 기준은 전체 데이터가 아니라 **학습 데이터에서만 계산**한 뒤 검증·테스트 데이터에 적용해야 정보 누수를 막을 수 있다.

### 3.2 중복값 처리

중복값(Duplicate)은 동일한 관측값이 두 번 이상 저장된 경우를 말한다. 시스템 재전송, 데이터 병합 오류 또는 반복 입력 등으로 발생할 수 있다.

#### 처리 과정

1. 전체 열 또는 식별에 필요한 핵심 열을 기준으로 중복을 찾는다.
2. 완전한 중복인지, 의미 있는 반복 관측인지 확인한다.
3. 잘못 생성된 중복만 제거하고 필요한 기록은 유지한다.

같은 고객이 여러 번 구매한 기록처럼 값이 비슷해도 실제로 다른 사건일 수 있다. 따라서 단순히 모든 열이 같거나 ID가 같다는 이유만으로 삭제해서는 안 된다.

중복 데이터가 학습 세트와 테스트 세트에 나뉘어 들어가면 모델이 동일한 사례를 이미 본 상태에서 평가되어 성능이 과대평가될 수 있다.

### 3.3 이상치 처리

이상치(Outlier)는 다른 관측값과 비교해 매우 크거나 작아 일반적인 패턴에서 벗어난 값이다.

#### 탐지 방법

- 박스플롯과 산점도 확인
- 사분위 범위(IQR) 기준 사용
- 평균과 표준편차를 이용한 Z-score
- 도메인 규칙 확인
- Isolation Forest 등 이상 탐지 모델 사용

#### 처리 방법

| 상황 | 처리 방법 |
|---|---|
| 입력·측정 오류 | 원본을 확인해 수정하거나 제거 |
| 값은 유효하지만 극단적임 | 로그 변환, 구간화 또는 상·하한 처리 |
| 중요한 희귀 사례 | 제거하지 않고 별도 분석 |
| 모델이 이상치에 민감함 | 중앙값, RobustScaler, 트리 모델 등 강건한 방법 사용 |

이상치는 데이터 오류일 수도 있지만 사기 거래나 기계 고장처럼 분석의 핵심 대상일 수도 있다. 따라서 통계 기준을 넘었다는 이유만으로 자동 삭제하지 말고 발생 원인과 분석 목적을 확인해야 한다.

> **한 문장 정리:** 결측값은 삭제 또는 합리적인 값으로 대체하고, 중복값은 실제 반복 관측인지 확인한 뒤 제거하며, 이상치는 오류와 의미 있는 희귀 사례를 구분해 처리해야 한다.

---

## 4. 지도 학습과 비지도 학습의 차이

### 지도 학습

지도 학습(Supervised Learning)은 입력 데이터와 정답인 라벨을 함께 사용하여 입력과 정답의 관계를 학습한다.

- **분류:** 스팸 여부, 질병 유무, 고객 이탈 여부 예측
- **회귀:** 주택 가격, 매출, 수요량 예측
- **대표 모델:** 선형·로지스틱 회귀, 결정 트리, SVM, 신경망

### 비지도 학습

비지도 학습(Unsupervised Learning)은 정답 라벨 없이 데이터 자체에 존재하는 구조와 패턴을 찾는다.

- **군집화:** 비슷한 고객을 여러 그룹으로 분류
- **차원 축소:** 많은 변수를 적은 차원으로 압축
- **이상 탐지:** 일반적인 패턴과 다른 데이터 발견
- **대표 모델:** K-means, DBSCAN, PCA, 계층적 군집화

### 핵심 비교

| 구분 | 지도 학습 | 비지도 학습 |
|---|---|---|
| 정답 라벨 | 있음 | 없음 |
| 목적 | 새로운 데이터의 정답 예측 | 숨겨진 구조와 패턴 발견 |
| 대표 문제 | 분류, 회귀 | 군집화, 차원 축소 |
| 평가 | 실제 정답과 예측을 비교 | 명확한 정답이 없어 평가가 상대적으로 어려움 |

> **한 문장 정리:** 지도 학습은 정답이 있는 데이터로 예측 규칙을 학습하고, 비지도 학습은 정답 없이 데이터 내부의 구조와 패턴을 찾는다.

---

## 5. 손실 함수란 무엇이며, 왜 중요한가?

손실 함수(Loss Function)는 모델의 예측값과 실제값이 얼마나 다른지를 하나의 숫자로 나타내는 함수이다. 학습 과정은 일반적으로 손실 함수의 값을 줄이는 방향으로 모델의 파라미터를 업데이트한다.

### 대표적인 손실 함수

| 문제 | 대표 손실 함수 | 특징 |
|---|---|---|
| 회귀 | 평균제곱오차(MSE) | 큰 오차에 더 큰 불이익을 부여 |
| 회귀 | 평균절대오차(MAE) | MSE보다 이상치의 영향을 덜 받음 |
| 이진 분류 | Binary Cross-Entropy | 두 클래스의 확률 예측을 평가 |
| 다중 분류 | Categorical Cross-Entropy | 여러 클래스의 확률 예측을 평가 |

### 중요한 이유

- 모델이 무엇을 좋은 예측으로 볼지 결정한다.
- 가중치를 업데이트할 방향을 제공한다.
- 문제에 맞지 않는 손실 함수는 잘못된 방향의 학습을 유도할 수 있다.
- 클래스 불균형이나 이상치 등 데이터의 특성을 학습에 반영할 수 있다.

손실 함수와 평가지표는 관련되지만 역할이 다르다. 손실 함수는 학습을 위해 미분 가능하고 최적화하기 쉬운 형태가 필요하다. 평가지표는 정확도나 F1-score처럼 사람이 모델 성능을 해석하는 기준이다.

> **한 문장 정리:** 손실 함수는 예측과 실제값의 차이를 수치화해 모델이 어떤 방향으로 파라미터를 수정할지 알려주는 학습의 기준이다.

---

## 6. 편향과 분산 및 두 개념의 관계

### 편향

편향(Bias)은 모델의 평균적인 예측이 실제 규칙에서 얼마나 벗어나 있는지를 나타낸다.

- 편향이 높으면 모델이 너무 단순해 데이터의 패턴을 충분히 학습하지 못한다.
- 학습 데이터와 테스트 데이터에서 모두 성능이 낮은 **과소적합**이 나타날 수 있다.
- 예: 복잡한 곡선 관계를 단순한 직선으로만 표현하는 모델

### 분산

분산(Variance)은 학습 데이터가 조금 달라졌을 때 모델의 예측이 얼마나 변하는지를 나타낸다.

- 분산이 높으면 모델이 학습 데이터의 세부적인 잡음까지 학습한다.
- 학습 성능은 높지만 새로운 데이터의 성능이 낮은 **과적합**이 나타날 수 있다.
- 예: 깊이가 제한되지 않은 결정 트리

### 편향과 분산의 관계

| 모델 상태 | 편향 | 분산 | 결과 |
|---|---|---|---|
| 지나치게 단순함 | 높음 | 낮음 | 과소적합 |
| 적절한 복잡도 | 적절함 | 적절함 | 일반화 성능이 좋음 |
| 지나치게 복잡함 | 낮음 | 높음 | 과적합 |

모델을 복잡하게 하면 편향은 줄어드는 대신 분산이 증가하는 경향이 있고, 모델을 단순하게 하면 분산은 줄지만 편향이 커질 수 있다. 이를 **편향-분산 트레이드오프**라고 한다.

두 값을 모두 무조건 최소화하기보다는 검증 성능을 기준으로 적절한 모델 복잡도를 찾아야 한다. 교차 검증, 규제, 가지치기와 앙상블 등을 통해 균형을 조절할 수 있다.

> **한 문장 정리:** 편향은 모델이 실제 패턴을 충분히 배우지 못한 오차이고 분산은 학습 데이터 변화에 지나치게 민감한 정도이며, 좋은 모델은 두 값 사이의 균형을 찾아야 한다.

---

## 7. K-폴드 교차 검증에서 K를 선택할 때 고려할 점

K-폴드 교차 검증은 데이터를 K개의 폴드로 나누고, 한 폴드를 검증에 사용하고 나머지를 학습에 사용하는 과정을 K번 반복한다. 각 폴드가 한 번씩 검증 데이터가 되며, K개의 평가 결과를 평균하여 모델의 일반화 성능을 추정한다.

### K가 작을 때

- 한 번의 검증 세트가 커진다.
- 학습 횟수가 적어 계산 비용이 낮다.
- 각 모델이 사용하는 학습 데이터가 적어 성능을 다소 낮게 추정할 수 있다.

### K가 클 때

- 각 모델이 더 많은 데이터를 학습에 사용한다.
- 모델을 K번 학습하므로 계산 비용이 증가한다.
- 폴드마다 모델이 매우 비슷해져 평가값의 변동성이 커질 수 있다.
- 극단적으로 K가 전체 샘플 수와 같으면 LOOCV가 된다.

### 선택할 때 고려할 사항

| 고려 사항 | 내용 |
|---|---|
| 데이터 크기 | 데이터가 적으면 큰 K로 학습 데이터를 더 확보할 수 있음 |
| 계산 비용 | 학습이 오래 걸리는 모델은 K가 클수록 부담이 커짐 |
| 클래스 비율 | 분류에서는 각 폴드의 비율을 유지하는 Stratified K-Fold 사용 |
| 최소 클래스 수 | K가 소수 클래스의 샘플 수보다 크지 않도록 주의 |
| 그룹 구조 | 같은 사람·회사 등의 데이터는 Group K-Fold로 분리 |
| 시간 순서 | 시계열 데이터에는 일반 K-Fold 대신 시간 순서를 보존하는 분할 사용 |
| 평가 안정성 | 한 번의 K-Fold 결과만 보지 않고 필요하면 반복 교차 검증 고려 |

실무에서는 성능 추정과 계산 비용의 균형이 비교적 좋은 **5 또는 10**을 자주 사용한다. 그러나 K 자체보다 데이터의 클래스, 그룹과 시간 구조에 맞는 분할 방식을 사용하는 것이 더 중요하다.

> **한 문장 정리:** K가 크면 더 많은 데이터를 학습에 사용할 수 있지만 계산 비용과 평가 변동성이 커질 수 있으므로, 데이터 크기·클래스 수·연산 비용과 데이터 구조를 고려해 보통 5 또는 10을 선택한다.

---

## 8. 최종 정리

- 선형대수학은 머신러닝의 데이터와 모델 연산을 표현하는 기초 언어이다.
- EDA는 모델링 전에 데이터의 구조, 분포, 관계와 품질을 탐색하는 과정이다.
- 결측값, 중복값과 이상치는 원인과 의미를 확인한 뒤 처리해야 한다.
- 지도 학습은 라벨을 이용하고 비지도 학습은 라벨 없이 패턴을 찾는다.
- 손실 함수는 모델이 줄여야 할 예측 오차를 정의한다.
- 편향과 분산의 균형은 모델의 일반화 성능을 결정한다.
- K-폴드의 K는 데이터 규모와 구조 및 계산 비용을 고려해 선택해야 한다.

---

## 9. 참고 자료

1. Ian Goodfellow, Yoshua Bengio, Aaron Courville, [Deep Learning — Linear Algebra](https://www.deeplearningbook.org/contents/linear_algebra.html)
2. NIST/SEMATECH, [What is Exploratory Data Analysis?](https://www.itl.nist.gov/div898/handbook/eda/section1/eda11.htm)
3. scikit-learn, [Imputation of Missing Values](https://scikit-learn.org/stable/modules/impute.html)
4. pandas, [DataFrame.drop_duplicates](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.drop_duplicates.html)
5. scikit-learn, [Supervised Learning](https://scikit-learn.org/stable/supervised_learning.html)
6. scikit-learn, [Unsupervised Learning](https://scikit-learn.org/stable/unsupervised_learning.html)
7. Keras, [Losses](https://keras.io/api/losses/)
8. scikit-learn, [Bias-Variance Decomposition](https://scikit-learn.org/stable/auto_examples/ensemble/plot_bias_variance.html)
9. scikit-learn, [Cross-validation](https://scikit-learn.org/stable/modules/cross_validation.html)
10. scikit-learn, [KFold](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.KFold.html)
