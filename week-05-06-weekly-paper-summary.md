# 위클리 페이퍼 #5, #6

> **주제:** PyTorch와 TensorFlow, 텐서, CNN, 오토 인코더  
> **작성자:** 이름을 입력하세요  
> **작성일:** 2026년 8월 10일

---

## 1. PyTorch와 TensorFlow 비교 (선택)

PyTorch와 TensorFlow는 모두 딥러닝 모델을 만들고 학습하기 위한 오픈소스 프레임워크이다. 두 프레임워크 모두 텐서 연산, 자동 미분, GPU·가속기 사용, 분산 학습과 모델 저장 기능을 제공한다.

### 핵심 비교

| 비교 기준 | PyTorch | TensorFlow |
|---|---|---|
| 기본 구성 방식 | `nn.Module`과 학습 루프를 직접 구성하는 방식이 익숙함 | 고수준 API인 Keras로 모델과 학습 과정을 빠르게 구성 가능 |
| 실행 방식 | 일반 Python처럼 즉시 실행하며 `torch.compile`로 최적화 가능 | 즉시 실행이 기본이며 `tf.function`으로 그래프 실행 가능 |
| 코드 제어 | 학습 루프와 모델 동작을 세밀하게 변경하기 편리함 | `Model.fit()`을 이용한 표준 학습과 사용자 정의 학습을 모두 지원 |
| 디버깅 | Python 코드 흐름을 그대로 따라가기 쉬움 | Eager 모드는 직관적이지만 `tf.function` 내부 디버깅은 추가 이해가 필요할 수 있음 |
| 분산 학습 | `torch.distributed` 계열 기능 제공 | `tf.distribute.Strategy`와 Keras가 통합되어 있음 |
| 주요 장점 | 유연한 모델 연구와 사용자 정의 실험에 편리 | Keras와 결합한 표준 학습 파이프라인 구성에 편리 |

과거에는 PyTorch는 연구용, TensorFlow는 서비스용이라는 구분이 자주 사용되었다. 그러나 현재는 두 프레임워크 모두 연구와 실제 서비스에 필요한 기능을 제공하므로 이분법적으로 구분하기 어렵다.

### 선택 기준

- Python다운 코드와 세밀한 학습 루프 제어가 중요하면 PyTorch가 편리할 수 있다.
- Keras의 고수준 API와 기존 TensorFlow 생태계를 활용한다면 TensorFlow가 편리할 수 있다.
- 이미 사용 중인 모델, 라이브러리, 배포 환경과 팀 경험도 함께 고려해야 한다.
- 단순히 어느 프레임워크가 더 우수한지를 따지기보다 프로젝트 조건에 맞는 도구를 선택하는 것이 중요하다.

> **한 문장 정리:** PyTorch와 TensorFlow는 핵심 기능이 비슷한 딥러닝 프레임워크이며, PyTorch는 유연한 Python 중심 제어에, TensorFlow는 Keras 기반의 표준화된 학습 구성에 강점이 있다.

---

## 2. PyTorch의 텐서와 NumPy 배열의 차이 (필수)

### 텐서란?

PyTorch의 텐서(Tensor)는 하나의 자료형을 가진 숫자를 여러 차원으로 배열한 데이터 구조이다. 딥러닝에서는 입력 데이터, 모델의 가중치, 중간 계산 결과와 출력값을 모두 텐서로 표현한다.

```text
0차원 텐서: 숫자 하나
1차원 텐서: 벡터
2차원 텐서: 행렬
3차원 이상: 이미지 묶음, 영상, 시계열 등의 고차원 데이터
```

예를 들어 컬러 이미지 여러 장은 일반적으로 `배치 × 채널 × 높이 × 너비` 형태의 4차원 텐서로 표현한다.

### NumPy 배열과의 공통점

- 여러 차원의 숫자 데이터를 저장한다.
- 인덱싱, 슬라이싱과 모양 변경을 지원한다.
- 덧셈, 곱셈과 행렬 연산을 수행할 수 있다.
- `shape`와 `dtype` 같은 속성을 가진다.

### 차이점

| 비교 기준 | PyTorch Tensor | NumPy Array |
|---|---|---|
| 주요 목적 | 딥러닝 모델의 학습과 가속 연산 | 범용 수치 계산과 데이터 처리 |
| 실행 장치 | CPU뿐 아니라 GPU와 기타 가속기 사용 가능 | 기본 NumPy 배열은 주로 CPU에서 연산 |
| 자동 미분 | `requires_grad=True`로 연산 기록과 기울기 계산 가능 | 자체적인 자동 미분 기능이 없음 |
| 딥러닝 연결 | 신경망 레이어, 손실 함수와 옵티마이저에 직접 사용 | 모델에 사용하려면 보통 텐서로 변환 필요 |
| 데이터 이동 | `.to(device)` 등으로 장치 사이를 이동 | 장치 이동 개념이 없음 |

### 상호 변환 시 주의할 점

```python
import numpy as np
import torch

array = np.array([1, 2, 3])
tensor = torch.from_numpy(array)   # 메모리를 공유할 수 있음

cpu_array = tensor.numpy()        # CPU 텐서를 NumPy 배열로 변환
gpu_tensor = tensor.to("cuda")    # 사용 가능한 경우 GPU로 이동
```

`torch.from_numpy()`로 만든 텐서와 원본 배열은 메모리를 공유할 수 있다. 따라서 한쪽의 값을 변경하면 다른 쪽에도 반영될 수 있다. 반면 `torch.tensor(array)`는 데이터를 복사해 새 텐서를 만든다.

GPU 텐서는 곧바로 NumPy 배열로 변환할 수 없다. 먼저 CPU로 옮긴 뒤 필요하다면 계산 그래프와의 연결을 끊어야 한다.

```python
array = gpu_tensor.detach().cpu().numpy()
```

> **한 문장 정리:** PyTorch 텐서는 NumPy 배열과 비슷한 다차원 데이터 구조이지만, GPU 연산과 자동 미분을 지원하여 딥러닝 학습에 직접 사용할 수 있다는 차이가 있다.

---

## 3. CNN을 구성하는 각 레이어의 역할 (필수)

CNN(Convolutional Neural Network)은 이미지처럼 공간적 구조를 가진 데이터에서 지역적인 특징을 찾는 데 적합한 신경망이다. 일반적인 이미지 분류 CNN은 다음과 같은 흐름을 가진다.

```text
입력 → 합성곱 → 활성화 → 풀링 → 합성곱 → 활성화 → 풀링
     → Flatten 또는 Global Average Pooling → 완전연결 → 출력
```

모든 CNN이 아래 레이어를 전부 사용하는 것은 아니며, 모델 구조에 따라 일부 레이어가 생략되거나 다른 방식으로 대체될 수 있다.

### 3.1 입력 레이어

이미지를 모델이 처리할 수 있는 텐서 형태로 전달한다. 컬러 이미지 한 장은 높이, 너비와 RGB 채널을 가지며, 학습할 때는 여기에 배치 차원이 추가된다.

**역할:** 입력 크기와 채널 수를 정의하고 픽셀 데이터를 다음 레이어로 전달한다.

### 3.2 합성곱 레이어

합성곱 레이어(Convolution Layer)는 작은 필터를 이미지 위에서 이동시키며 지역적인 패턴을 찾는다. 필터마다 서로 다른 특징을 학습하며 결과로 특징 지도(Feature Map)를 만든다.

- 초기 레이어: 선, 모서리, 색 변화 같은 단순한 특징 감지
- 중간 레이어: 질감, 곡선과 부분적인 형태 감지
- 깊은 레이어: 눈, 바퀴, 얼굴과 같은 복잡한 특징 감지

같은 필터를 이미지 전체에 반복해서 사용하므로 완전연결층보다 적은 파라미터로 위치가 다른 동일한 패턴을 탐지할 수 있다.

**역할:** 이미지의 지역적인 특징을 추출한다.

### 3.3 활성화 함수

ReLU 같은 활성화 함수는 음수 값을 0으로 만드는 등의 비선형 변환을 적용한다. 활성화 함수가 없다면 합성곱 레이어를 여러 개 연결해도 전체는 하나의 선형 변환과 비슷해져 복잡한 패턴을 표현하기 어렵다.

**역할:** 모델에 비선형성을 추가해 복잡한 특징을 학습하게 한다.

### 3.4 배치 정규화 레이어

배치 정규화(Batch Normalization)는 미니배치의 중간 출력 분포를 조정하고 학습 가능한 크기와 이동 값을 적용한다. 모델에 따라 학습을 더 안정적으로 만들고 비교적 큰 학습률을 사용하는 데 도움을 줄 수 있다.

**역할:** 중간 활성값의 규모를 조정해 학습 안정성을 높인다.

### 3.5 풀링 레이어

풀링 레이어(Pooling Layer)는 특징 지도의 가로와 세로 크기를 줄인다. Max Pooling은 일정 영역에서 가장 큰 값을 선택하고, Average Pooling은 평균값을 사용한다.

공간 크기가 줄어들면 계산량이 감소하고 작은 위치 변화에 덜 민감해질 수 있다. 다만 지나친 풀링은 위치 정보를 많이 잃게 한다.

**역할:** 중요한 특징을 요약하면서 공간 크기와 계산량을 줄인다.

### 3.6 Dropout 레이어

Dropout은 학습 중 일부 뉴런의 출력을 무작위로 0으로 만든다. 특정 뉴런에 지나치게 의존하는 것을 막아 과적합을 완화할 수 있으며, 추론할 때는 비활성화된다.

**역할:** 모델의 과적합을 줄인다.

### 3.7 Flatten 또는 Global Average Pooling

- **Flatten:** 여러 특징 지도를 하나의 긴 1차원 벡터로 펼친다.
- **Global Average Pooling:** 각 특징 지도의 전체 평균을 구해 채널별 값 하나로 줄인다.

Flatten은 많은 정보를 유지하지만 뒤의 완전연결층 파라미터 수가 크게 늘 수 있다. Global Average Pooling은 파라미터 수와 과적합 위험을 줄이는 데 유리하다.

**역할:** 추출된 특징을 최종 분류 레이어가 사용할 수 있는 형태로 바꾼다.

### 3.8 완전연결 레이어

완전연결 레이어(Fully Connected 또는 Dense Layer)는 앞에서 추출한 특징들을 종합해 클래스별 점수나 최종 예측값을 계산한다.

**역할:** 추출된 여러 특징을 결합해 최종 판단을 내린다.

### 3.9 출력 레이어

문제 유형에 맞는 결과를 출력한다.

- 다중 분류: 클래스별 점수 또는 확률
- 이진 분류: 하나의 점수 또는 확률
- 회귀: 연속적인 예측값

학습 과정에서는 출력과 실제 정답을 손실 함수로 비교하고, 역전파를 통해 앞선 레이어의 필터와 가중치를 수정한다.

> **한 문장 정리:** CNN은 합성곱으로 특징을 찾고, 활성화 함수로 복잡한 패턴을 표현하며, 풀링으로 특징을 요약한 뒤 완전연결층에서 최종 예측을 수행한다.

---

## 4. 발표용 추가 정리: CNN 레이어의 역할

### 가장 쉽게 설명하는 핵심 흐름

CNN을 **사진을 단계적으로 조사하는 탐정팀**에 비유할 수 있다.

1. **합성곱 레이어는 탐정**처럼 사진의 작은 영역을 살피며 선과 모서리 같은 단서를 찾는다.
2. **ReLU는 단서 선별기**처럼 도움이 되는 반응을 남겨 비선형적인 특징을 표현하게 한다.
3. **풀링 레이어는 요약 담당자**처럼 중요한 단서만 남기고 지도의 크기를 줄인다.
4. 이 과정을 반복하면 단순한 선들이 질감과 부품으로, 부품들이 얼굴이나 자동차 같은 객체 특징으로 결합된다.
5. **완전연결 레이어는 최종 판정자**처럼 모인 특징을 보고 어느 클래스인지 결정한다.

### 크기 변화로 이해하는 예시

```text
입력 이미지       32 × 32 × 3
합성곱 결과       32 × 32 × 32  : 32종류의 특징 탐지
Max Pooling       16 × 16 × 32  : 가로·세로를 절반으로 요약
두 번째 합성곱   16 × 16 × 64  : 더 복잡한 특징 탐지
두 번째 풀링      8 × 8 × 64   : 다시 공간 크기 축소
특징 종합                       : Flatten 또는 평균으로 변환
출력                           : 고양이, 강아지 등의 클래스 점수
```

합성곱 뒤의 정확한 크기는 필터 크기, 패딩과 스트라이드 설정에 따라 달라질 수 있다. 위 예시는 패딩으로 합성곱 전후의 가로·세로 크기를 유지한 경우이다.

### 2분 발표 대본

> CNN은 이미지에서 특징을 단계적으로 추출해 분류하는 신경망입니다. 일반적인 흐름은 합성곱, 활성화 함수, 풀링, 그리고 완전연결 레이어 순서입니다.
>
> 먼저 합성곱 레이어는 작은 필터를 이미지 전체에 이동시키면서 선, 모서리와 같은 지역적인 특징을 찾고 특징 지도를 만듭니다. 같은 필터를 모든 위치에 사용하기 때문에 이미지에서 특징이 어디에 있더라도 찾을 수 있고, 완전연결층보다 적은 파라미터를 사용할 수 있습니다.
>
> 다음 ReLU 활성화 함수는 비선형성을 추가합니다. 덕분에 여러 레이어를 쌓았을 때 단순한 선형 변환을 넘어서 복잡한 패턴을 표현할 수 있습니다.
>
> 풀링 레이어는 일정 영역의 최댓값이나 평균값을 선택해 특징 지도의 크기를 줄입니다. 이를 통해 계산량을 줄이고 작은 위치 변화에 덜 민감하게 만들 수 있습니다.
>
> 이러한 과정을 반복하면 앞쪽 레이어의 선과 모서리가 뒤쪽에서는 질감, 눈, 바퀴와 같은 복잡한 특징으로 결합됩니다. 마지막에는 Flatten이나 Global Average Pooling으로 특징을 정리하고, 완전연결 레이어가 이를 종합하여 최종 클래스를 예측합니다.
>
> 정리하면 CNN은 합성곱으로 특징을 찾고, 활성화 함수로 복잡성을 더하며, 풀링으로 요약한 뒤 완전연결층에서 최종 판단을 내리는 구조입니다.

### 예상 추가 질문과 답변

**Q. 합성곱층을 여러 개 사용하는 이유는 무엇인가요?**  
앞쪽에서는 선과 모서리 같은 단순한 특징을 찾고, 뒤쪽에서는 이 특징들을 조합해 질감이나 객체의 일부처럼 더 복잡한 특징을 만들기 위해서이다.

**Q. 풀링은 반드시 필요한가요?**  
반드시 필요한 것은 아니다. 스트라이드가 큰 합성곱으로 크기를 줄이거나 마지막에 Global Average Pooling을 사용하는 모델도 있다.

**Q. ReLU가 없으면 어떻게 되나요?**  
선형 연산을 여러 번 연결한 것과 비슷해져 복잡한 비선형 관계를 표현하는 능력이 제한된다.

**Q. Flatten과 Global Average Pooling의 차이는 무엇인가요?**  
Flatten은 모든 값을 길게 펼쳐 정보와 파라미터 수가 많아질 수 있고, Global Average Pooling은 채널마다 평균값 하나만 남겨 더 간결하다.

**Q. CNN이 완전연결 신경망보다 이미지에 적합한 이유는 무엇인가요?**  
지역적인 픽셀 관계를 활용하고 동일한 필터를 여러 위치에서 공유하므로 이미지의 공간 구조를 유지하면서 파라미터 수를 줄일 수 있기 때문이다.

---

## 5. 오토 인코더와 적용 상황 (선택)

### 오토 인코더란?

오토 인코더(Autoencoder)는 입력 데이터를 압축된 표현으로 바꾼 뒤 다시 원래 입력과 비슷하게 복원하도록 학습하는 신경망이다. 학습 목표가 입력 자체이므로 별도의 정답 라벨 없이 학습할 수 있다.

```text
입력 x → 인코더 → 잠재 표현 z → 디코더 → 복원 결과
```

모델이 단순히 입력을 복사하지 못하도록 잠재 표현의 크기를 제한하거나 잡음, 희소성, 규제 등의 제약을 사용한다. 이 과정에서 데이터의 중요한 특징을 담은 표현을 학습하도록 유도한다.

### 인코더와 디코더 비교

| 구분 | 인코더 | 디코더 |
|---|---|---|
| 입력 | 원본 데이터 | 잠재 표현 |
| 출력 | 압축된 잠재 표현 | 복원된 데이터 |
| 목적 | 중요한 특징을 추출하고 압축 | 잠재 표현에서 원본과 비슷한 결과 생성 |
| 정보 흐름 | 고차원에서 저차원 표현으로 | 저차원 표현에서 원래 공간으로 |

### 적용하기 적합한 상황

#### 1) 차원 축소와 특징 학습

고차원 데이터를 작은 잠재 벡터로 표현하고 싶을 때 사용할 수 있다. 비선형 활성화 함수를 사용하면 PCA보다 복잡한 비선형 구조를 학습할 수 있다.

#### 2) 이미지 잡음 제거

잡음이 섞인 데이터를 입력하고 깨끗한 데이터를 복원하도록 학습하는 Denoising Autoencoder를 사용할 수 있다. 중요한 구조를 보존하면서 잡음을 제거하는 방법을 학습한다.

#### 3) 이상 탐지

정상 데이터만으로 오토 인코더를 학습하면 정상 패턴은 잘 복원하지만 학습하지 않은 이상 패턴은 복원 오류가 클 수 있다. 이 복원 오류를 이상 점수로 사용할 수 있다.

#### 4) 데이터 압축 표현과 검색

원본보다 작은 잠재 표현을 저장하거나, 잠재 공간에서 비슷한 데이터끼리 비교하는 데 활용할 수 있다. 다만 오토 인코더가 항상 범용 압축 알고리즘보다 효율적인 것은 아니다.

### 한계와 주의점

- 모델의 표현력이 지나치게 크면 중요한 특징 대신 입력을 그대로 복사할 수 있다.
- 낮은 복원 오류가 반드시 좋은 표현이나 높은 예측 성능을 의미하지는 않는다.
- 이상 데이터도 잘 복원하는 모델이라면 복원 오류 기반 이상 탐지가 실패할 수 있다.
- 일반 오토 인코더의 잠재 공간에서 임의 값을 뽑는다고 항상 자연스러운 데이터가 생성되지는 않는다. 생성 목적에는 VAE 같은 변형을 고려할 수 있다.

> **한 문장 정리:** 오토 인코더의 인코더는 입력을 중요한 잠재 표현으로 압축하고, 디코더는 그 표현을 원래 데이터와 비슷하게 복원하며, 차원 축소·잡음 제거·이상 탐지 등에 활용할 수 있다.

---

## 6. 최종 정리

- PyTorch와 TensorFlow는 모두 자동 미분과 가속기 연산을 지원하는 딥러닝 프레임워크이다.
- PyTorch 텐서는 NumPy 배열과 비슷하지만 GPU 연산과 자동 미분을 지원한다.
- CNN은 합성곱으로 특징을 추출하고 활성화 함수와 풀링을 거쳐 최종 클래스를 예측한다.
- 오토 인코더는 인코더로 중요한 표현을 만들고 디코더로 입력을 복원한다.
- 발표에서는 CNN을 `특징 탐지 → 선택 → 요약 → 최종 판단`의 흐름으로 설명하면 이해하기 쉽다.

---

## 7. 참고 자료

1. PyTorch, [Tensors](https://docs.pytorch.org/tutorials/beginner/basics/tensorqs_tutorial.html)
2. PyTorch, [torch.Tensor](https://docs.pytorch.org/docs/stable/tensors.html)
3. PyTorch, [Neural Networks Tutorial](https://docs.pytorch.org/tutorials/beginner/blitz/neural_networks_tutorial.html)
4. PyTorch, [torch.compile](https://docs.pytorch.org/docs/stable/generated/torch.compile.html)
5. TensorFlow, [Keras: The High-Level API for TensorFlow](https://www.tensorflow.org/guide/keras)
6. TensorFlow, [Distributed Training](https://www.tensorflow.org/guide/distributed_training)
7. TensorFlow, [Convolutional Neural Network Tutorial](https://www.tensorflow.org/tutorials/images/cnn)
8. Ian Goodfellow, Yoshua Bengio, Aaron Courville, [Deep Learning — Autoencoders](https://www.deeplearningbook.org/contents/autoencoders.html)

