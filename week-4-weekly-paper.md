# 4주차 위클리 페이퍼

> **주제:** 딥러닝과 머신러닝의 관계, 딥러닝 하이퍼파라미터  
> **작성자:** 이름을 입력하세요  
> **작성일:** 2026년 7월 27일

---

## 1. 딥러닝과 머신러닝의 포함관계

### 머신러닝이란?

머신러닝(Machine Learning)은 컴퓨터가 사람이 모든 규칙을 직접 작성하지 않아도 데이터에서 패턴을 학습하고 예측이나 의사결정을 수행하게 하는 기술이다.

대표적인 머신러닝 모델에는 다음과 같은 것들이 있다.

- 선형 회귀와 로지스틱 회귀
- 결정 트리와 랜덤 포레스트
- 서포트 벡터 머신(SVM)
- K-최근접 이웃(KNN)
- 군집화 알고리즘
- 인공신경망

### 딥러닝이란?

딥러닝(Deep Learning)은 여러 층으로 구성된 인공신경망을 이용해 데이터의 표현과 예측 규칙을 함께 학습하는 머신러닝의 한 분야이다.

이미지 인식에서는 낮은 층이 선이나 모서리 같은 단순한 특징을 학습하고, 더 깊은 층이 형태나 객체처럼 복잡한 특징을 학습할 수 있다. 이처럼 데이터에서 필요한 표현을 여러 단계에 걸쳐 학습하는 것이 딥러닝의 핵심이다.

### 포함관계

```text
머신러닝
├── 선형·로지스틱 회귀
├── 결정 트리·랜덤 포레스트
├── SVM·KNN
├── 군집화
└── 딥러닝
    ├── CNN
    ├── RNN·LSTM
    ├── Transformer
    └── 오토인코더
```

따라서 **모든 딥러닝은 머신러닝에 포함되지만, 모든 머신러닝이 딥러닝인 것은 아니다.**

### 머신러닝과 딥러닝 비교

| 비교 기준 | 전통적인 머신러닝 | 딥러닝 |
|---|---|---|
| 포함관계 | 더 넓은 개념 | 머신러닝의 하위 분야 |
| 대표 모델 | 회귀, 결정 트리, SVM, KNN | CNN, RNN, Transformer |
| 특징 추출 | 사람이 유용한 특성을 설계하는 경우가 많음 | 신경망이 데이터 표현을 함께 학습 |
| 적합한 데이터 | 표 형태의 정형 데이터에서 강점 | 이미지, 음성, 텍스트 같은 비정형 데이터에서 강점 |
| 데이터와 연산량 | 비교적 적은 데이터와 자원으로도 학습 가능 | 일반적으로 더 많은 데이터와 연산 자원이 필요 |
| 해석 가능성 | 비교적 설명하기 쉬운 모델이 많음 | 깊고 복잡할수록 해석이 어려움 |
| 학습 시간 | 비교적 짧은 경우가 많음 | 모델 규모에 따라 매우 길어질 수 있음 |

이 차이는 절대적인 기준은 아니다. 데이터의 크기와 구조, 모델 설계에 따라 전통적인 머신러닝이 더 좋은 성능을 낼 수도 있다. 특히 표 형태의 데이터에서는 트리 기반 모델이 딥러닝보다 효율적일 수 있으므로 문제에 맞는 모델을 선택해야 한다.

> **한 문장 정리:** 딥러닝은 다층 신경망으로 데이터의 표현을 학습하는 머신러닝의 하위 분야이므로, 모든 딥러닝은 머신러닝이지만 모든 머신러닝이 딥러닝은 아니다.

---

## 2. 딥러닝 성능을 위한 하이퍼파라미터

### 파라미터와 하이퍼파라미터의 차이

- **파라미터(Parameter):** 학습 과정에서 모델이 데이터로부터 직접 업데이트하는 값이다. 신경망의 가중치와 편향이 대표적이다.
- **하이퍼파라미터(Hyperparameter):** 모델 구조와 학습 방법을 제어하기 위해 학습 전 또는 학습 과정에서 사람이 정하는 값이다.

하이퍼파라미터는 학습 속도뿐 아니라 모델의 표현력, 과적합, 메모리 사용량 및 최종 일반화 성능에 영향을 준다.

### 2.1 모델 구조 관련 하이퍼파라미터

| 종류 | 역할 | 조정 시 고려할 점 |
|---|---|---|
| 은닉층의 수 | 모델의 깊이와 표현력을 결정 | 너무 적으면 과소적합, 너무 많으면 비용과 과적합 위험 증가 |
| 층별 뉴런 수 | 각 층이 학습할 수 있는 정보량을 결정 | 많을수록 표현력과 파라미터 수가 증가 |
| 활성화 함수 | 신경망에 비선형성을 추가 | ReLU, GELU, sigmoid 등을 문제에 맞게 선택 |
| 모델 구조 | 데이터 처리 방식을 결정 | 이미지에는 CNN, 순차 데이터에는 RNN, 다양한 시퀀스에는 Transformer 등을 고려 |
| 구조별 설정 | 세부적인 모델 크기를 결정 | 필터 수, 커널 크기, 임베딩 차원, 어텐션 헤드 수 등이 해당 |

모델을 크게 만들면 항상 성능이 좋아지는 것은 아니다. 데이터가 적으면 복잡한 모델이 학습 데이터를 외울 수 있고, 학습 및 추론 비용도 증가한다.

### 2.2 학습과 최적화 관련 하이퍼파라미터

| 종류 | 역할 | 주의점 |
|---|---|---|
| 학습률 | 한 번의 업데이트에서 가중치를 얼마나 변경할지 결정 | 너무 크면 발산하고 너무 작으면 학습이 느리거나 지역적인 해에 머물 수 있음 |
| 옵티마이저 | 가중치를 업데이트하는 방법을 결정 | SGD, Adam, AdamW 등이 있으며 모든 문제에서 항상 최적인 하나의 방법은 없음 |
| 배치 크기 | 한 번의 업데이트에 사용하는 데이터 수 | 크면 메모리를 많이 사용하고, 작으면 기울기의 변동이 커짐 |
| 에포크·학습 스텝 | 전체 데이터를 반복해서 학습하는 횟수 | 부족하면 과소적합, 지나치면 과적합 가능 |
| 학습률 스케줄 | 학습이 진행됨에 따라 학습률을 변경 | 초기에는 빠르게 학습하고 후반에는 세밀하게 조정 가능 |
| 모멘텀·Adam 계수 | 이전 기울기와 이동평균의 반영 정도를 조절 | 옵티마이저와 학습률에 따라 적절한 값이 달라짐 |

이 가운데 **학습률은 가장 먼저 확인할 핵심 하이퍼파라미터**이다. 다른 설정이 좋아도 학습률이 지나치게 크면 손실이 불안정해지고, 너무 작으면 충분히 학습되지 않을 수 있다.

### 2.3 과적합 방지 관련 하이퍼파라미터

| 종류 | 역할 | 주의점 |
|---|---|---|
| Dropout 비율 | 학습 중 일부 뉴런을 무작위로 비활성화 | 너무 높으면 필요한 정보까지 잃어 과소적합 가능 |
| Weight decay | 가중치가 지나치게 커지는 것을 억제 | 너무 강하면 모델의 표현력이 제한될 수 있음 |
| Early stopping의 patience | 검증 성능이 개선되지 않을 때 기다릴 횟수 | 너무 짧으면 학습을 일찍 종료할 수 있음 |
| 데이터 증강의 종류와 강도 | 변형된 학습 데이터를 만들어 일반화 성능 향상 | 실제 데이터에서 불가능한 변형은 성능을 낮출 수 있음 |

Dropout은 학습 중 일부 입력을 지정된 비율로 0으로 만들어 과적합을 완화한다. Early stopping은 검증 손실 등이 일정 기간 개선되지 않으면 학습을 종료하여 불필요한 학습과 과적합을 줄인다.

### 2.4 문제와 데이터 처리 관련 설정

- 손실 함수의 종류
- 입력 이미지 크기 또는 시퀀스 길이
- 정규화 및 전처리 방법
- 데이터 샘플링 방식
- 분류 임계값
- 데이터 증강 방법

이 설정들도 모델의 성능에 큰 영향을 주기 때문에 넓은 의미에서 튜닝 대상에 포함할 수 있다. 특히 손실 함수와 평가지표는 실제 해결하려는 문제의 목표와 일치해야 한다.

### 주요 하이퍼파라미터의 영향

| 설정 | 지나치게 작을 때 | 지나치게 클 때 |
|---|---|---|
| 학습률 | 학습이 매우 느림 | 손실이 진동하거나 발산 |
| 배치 크기 | 학습이 불안정하고 처리 속도가 느릴 수 있음 | 메모리 사용 증가, 다른 설정의 재조정 필요 |
| 모델 크기 | 복잡한 패턴을 학습하지 못함 | 과적합 및 학습 비용 증가 |
| Dropout 비율 | 과적합 방지 효과가 약함 | 과소적합 가능 |
| Weight decay | 가중치 규제 효과가 약함 | 모델 표현력 감소 |
| 에포크 수 | 과소적합 | 과적합 및 자원 낭비 |

### 하이퍼파라미터 튜닝 방법

하이퍼파라미터는 테스트 데이터가 아니라 **검증 데이터의 성능**을 기준으로 선택해야 한다. 테스트 데이터를 반복적으로 확인하며 설정을 바꾸면 테스트 데이터에도 간접적으로 과적합될 수 있다.

일반적인 순서는 다음과 같다.

1. 단순한 모델과 기본 설정으로 기준 성능을 만든다.
2. 학습 곡선을 확인하며 학습률을 우선 조정한다.
3. 배치 크기와 옵티마이저를 조정한다.
4. 은닉층과 뉴런 수 등 모델의 크기를 조정한다.
5. 과적합이 나타나면 Dropout, weight decay, 데이터 증강을 조정한다.
6. Early stopping으로 가장 좋은 시점의 모델을 선택한다.
7. 최종 설정이 결정된 뒤 테스트 데이터로 한 번 평가한다.

값을 찾는 방법에는 수동 탐색, 그리드 탐색, 랜덤 탐색, 베이지안 최적화, Hyperband 등이 있다. KerasTuner 같은 도구를 사용하면 층의 수, 뉴런 수, 활성화 함수, Dropout 적용 여부와 학습률 등을 자동으로 탐색할 수 있다.

> **한 문장 정리:** 딥러닝의 성능을 높이려면 모델 구조, 학습률과 옵티마이저, 배치 크기와 학습 횟수, Dropout과 weight decay 같은 규제 설정을 검증 성능을 기준으로 함께 조정해야 한다.

---

## 3. 최종 정리

- 딥러닝은 머신러닝과 별개의 기술이 아니라 머신러닝에 포함되는 분야이다.
- 딥러닝은 다층 신경망을 이용해 예측 규칙과 데이터 표현을 함께 학습한다.
- 하이퍼파라미터는 모델 구조, 최적화, 규제 및 데이터 처리 설정으로 구분할 수 있다.
- 학습률은 모델의 수렴에 직접 영향을 주므로 우선적으로 확인해야 한다.
- 하이퍼파라미터는 하나씩 독립적으로 작용하지 않으므로 상호작용을 고려해야 한다.
- 최적의 값은 정답처럼 정해져 있지 않으며 검증 데이터와 반복 실험을 통해 찾아야 한다.

---

## 4. 참고 자료

1. Ian Goodfellow, Yoshua Bengio, Aaron Courville, [Deep Learning — Introduction](https://www.deeplearningbook.org/contents/intro.html)
2. PyTorch, [Optimizing Model Parameters](https://docs.pytorch.org/tutorials/beginner/basics/optimization_tutorial.html)
3. Google for Developers, [Deep Learning Tuning Playbook](https://developers.google.com/machine-learning/guides/deep-learning-tuning-playbook)
4. Google for Developers, [Guide for Starting a New Project](https://developers.google.com/machine-learning/guides/deep-learning-tuning-playbook/new-project)
5. Keras, [Getting Started with KerasTuner](https://keras.io/keras_tuner/getting_started/)
6. Keras, [Dropout Layer](https://keras.io/api/layers/regularization_layers/dropout/)
7. Keras, [EarlyStopping](https://keras.io/api/callbacks/early_stopping/)
