# 3주차 위클리 페이퍼

> **주제:** 결정 트리, 부스팅, PCA와 요인 분석  
> **작성자:** 이름을 입력하세요  
> **작성일:** 2026년 7월 27일

---

## 1. 결정 트리의 장점과 단점

### 결정 트리란?

결정 트리(Decision Tree)는 데이터의 특성에 대한 조건을 순서대로 적용해 데이터를 나누고, 마지막 리프 노드에서 결과를 예측하는 지도학습 모델이다. 분류와 회귀 문제에 모두 사용할 수 있다.

```text
연체 기록이 있는가?
├── 예: 대출 거절
└── 아니오: 소득이 기준 이상인가?
    ├── 예: 대출 승인
    └── 아니오: 대출 거절
```

### 장점

| 장점 | 설명 |
|---|---|
| 해석이 쉽다 | 예측 과정을 `if-else` 형태의 규칙으로 확인할 수 있다. |
| 전처리가 적다 | 일반적으로 정규화와 표준화가 필요하지 않다. |
| 비선형 관계를 학습한다 | 복잡한 관계와 특성 간 상호작용을 표현할 수 있다. |
| 활용 범위가 넓다 | 분류와 회귀 문제에 모두 사용할 수 있다. |
| 예측이 빠르다 | 학습 후에는 트리의 한 경로만 따라가면 된다. |

### 단점

| 단점 | 설명 |
|---|---|
| 과적합하기 쉽다 | 깊은 트리는 학습 데이터의 잡음까지 외울 수 있다. |
| 결과가 불안정하다 | 데이터가 조금만 바뀌어도 트리 구조가 크게 달라질 수 있다. |
| 전역 최적해를 보장하지 않는다 | 각 단계에서 가장 좋은 분할을 선택하는 탐욕적 방식을 사용한다. |
| 매끄러운 예측에 약하다 | 회귀 결과가 계단 형태이며 학습 범위 밖의 외삽에 약하다. |
| 불균형 데이터의 영향을 받는다 | 다수 클래스를 중심으로 규칙이 만들어질 수 있다. |

과적합은 `max_depth`, `min_samples_leaf`, 가지치기 등을 이용해 완화할 수 있다. 여러 트리를 결합한 랜덤 포레스트나 부스팅을 사용하는 것도 결정 트리의 불안정성을 줄이는 방법이다.

> **한 문장 정리:** 결정 트리는 해석이 쉽고 비선형 관계를 학습할 수 있지만, 과적합과 데이터 변화에 민감하다는 단점이 있다.

---

## 2. 부스팅의 특징과 주요 모델

### 부스팅이란?

부스팅(Boosting)은 성능이 낮은 여러 개의 약한 학습기를 **순차적으로 학습**하고 결합해 강한 모델을 만드는 앙상블 기법이다. 새로운 모델은 이전 모델이 틀린 부분을 집중적으로 보완한다.

배깅의 대표 모델인 랜덤 포레스트는 여러 트리를 독립적으로 학습해 결과를 평균내지만, 부스팅은 앞선 모델의 결과를 이용해 다음 모델을 학습한다.

### 핵심 특징

- 약한 학습기를 순차적으로 추가한다.
- 이전 모델의 오분류 또는 잔차에 집중한다.
- 여러 모델의 예측을 가중 결합한다.
- 복잡한 비선형 관계를 학습할 수 있다.
- 학습률과 트리 수를 적절하게 조절해야 한다.
- 순차 학습이므로 트리 사이의 완전한 병렬화가 어렵다.

### Gradient Boosting과 XGBoost

**Gradient Boosting**은 현재 모델의 손실을 줄이는 방향, 즉 손실 함수의 음의 기울기를 다음 모델이 학습하도록 한다.

**XGBoost**는 Gradient Boosting에 규제와 2차 미분 정보, 희소 데이터 처리 및 병렬 연산 등의 최적화를 추가한 모델이다.

### 그 밖의 부스팅 모델

> LightGBM과 CatBoost는 Gradient Boosting과 완전히 다른 계열이 아니라, GBDT를 각각 속도와 범주형 변수 처리 측면에서 개선한 모델이다.

#### 주요 모델 비교

| 모델 | 핵심 특징 | 장점 | 단점 |
|---|---|---|---|
| **AdaBoost** | 틀린 샘플의 가중치를 높여 다음 모델이 집중하게 한다. | 원리가 단순하고 조정할 파라미터가 적다. | 이상치와 잘못된 라벨에 민감하다. |
| **LightGBM** | 히스토그램과 leaf-wise 방식으로 트리를 빠르게 학습한다. | 속도가 빠르고 메모리 사용량이 적어 대규모 데이터에 적합하다. | 작은 데이터에서는 깊은 가지가 만들어져 과적합하기 쉽다. |
| **CatBoost** | 순서 기반 학습으로 범주형 특성과 타깃 누수를 처리한다. | 별도의 원-핫 인코딩 부담이 적고 범주형 변수가 많은 데이터에 강하다. | 순서 기반 통계 계산으로 시간과 메모리가 더 필요할 수 있다. |
| **NGBoost** | 하나의 값이 아니라 예측 확률분포를 학습한다. | 예측값과 함께 불확실성 및 예측 구간을 제공한다. | 확률분포를 적절히 선택해야 하며 학습이 더 복잡하다. |

### 모델 선택 기준

- 비교적 단순하고 깨끗한 데이터: **AdaBoost**
- 일반적인 정형 데이터의 강력한 기준 모델: **XGBoost**
- 대규모 데이터와 빠른 학습이 중요할 때: **LightGBM**
- 범주형 변수가 많을 때: **CatBoost**
- 예측의 불확실성까지 필요할 때: **NGBoost**

> **한 문장 정리:** 부스팅은 이전 모델의 실수를 다음 약한 학습기가 순차적으로 보완하도록 만들어 여러 모델을 하나의 강한 모델로 결합하는 방법이다.

---

## 3. PCA와 요인 분석의 차이

PCA와 요인 분석은 모두 많은 변수를 적은 수의 차원으로 표현할 수 있지만, 사용하는 목적이 다르다.

### 주성분 분석(PCA)

PCA(Principal Component Analysis)는 원래 변수들의 선형결합으로 새로운 축인 주성분을 만든다. 첫 번째 주성분은 데이터의 분산을 가장 많이 보존하는 방향이며, 이후의 주성분은 앞선 주성분과 직교하면서 남은 분산을 최대한 보존한다.

주요 목적은 다음과 같다.

- 차원 축소와 데이터 압축
- 상관된 변수의 중복 제거
- 고차원 데이터 시각화
- 머신러닝 모델의 입력 특성 생성

### 요인 분석(FA)

요인 분석(Factor Analysis)은 여러 관측변수의 상관관계가 직접 관측되지 않는 소수의 공통 요인에 의해 만들어졌다고 가정한다. 각 변수의 변동을 공통 요인이 설명하는 부분과 변수 고유의 변동 및 측정오차로 구분한다.

주요 목적은 다음과 같다.

- 변수들의 상관관계를 설명하는 잠재 요인 발견
- 설문 문항이 측정하는 공통 개념 탐색
- 측정도구의 구성 타당성 검토

### 핵심 비교

| 비교 기준 | PCA | 요인 분석 |
|---|---|---|
| 목적 | 정보 보존과 차원 축소 | 잠재 요인 발견과 상관관계 설명 |
| 분석 대상 | 전체 분산 | 변수들이 공유하는 공통분산 |
| 결과 | 관측변수의 선형결합인 주성분 | 관측되지 않는 잠재 요인 |
| 오차 처리 | 고유분산과 오차를 별도로 분리하지 않음 | 고유분산과 측정오차를 모형에 포함 |
| 축의 관계 | 표준 PCA의 주성분은 서로 직교 | 회전 방법에 따라 요인끼리 상관될 수 있음 |
| 주요 활용 | 압축, 시각화, 모델 전처리 | 심리·교육·마케팅 설문 분석 |

### 간단한 사례

키, 몸무게, 허리둘레처럼 상관된 측정값을 몇 개의 변수로 압축하려는 목적이라면 **PCA**가 적합하다.

여러 고객 설문 문항이 `서비스 만족도`, `가격 만족도`, `사용 편의성` 같은 잠재 개념으로 묶이는지 알아보려는 목적이라면 **요인 분석**이 적합하다.

> **한 문장 정리:** PCA는 전체 분산을 최대한 보존하는 데이터 압축 방법이고, 요인 분석은 관측변수의 공통분산을 설명하는 잠재 요인을 찾는 방법이다.

---

## 4. 최종 정리

- 결정 트리는 설명하기 쉽지만 과적합과 불안정성에 주의해야 한다.
- 부스팅은 약한 학습기를 순차적으로 학습해 이전 모델의 오류를 보완한다.
- LightGBM은 속도, CatBoost는 범주형 변수, NGBoost는 불확실성 예측에 강점이 있다.
- PCA는 데이터 압축, 요인 분석은 잠재 개념의 발견이 주된 목적이다.

---

## 5. 참고 자료

1. scikit-learn, [Decision Trees](https://scikit-learn.org/stable/modules/tree.html)
2. scikit-learn, [Ensemble Methods](https://scikit-learn.org/stable/modules/ensemble.html)
3. Tianqi Chen and Carlos Guestrin, [XGBoost: A Scalable Tree Boosting System](https://arxiv.org/abs/1603.02754)
4. Guolin Ke et al., [LightGBM: A Highly Efficient Gradient Boosting Decision Tree](https://papers.nips.cc/paper/2017/hash/6449f44a102fde848669bdd9eb6b76fa-Abstract.html)
5. Liudmila Prokhorenkova et al., [CatBoost: Unbiased Boosting with Categorical Features](https://proceedings.neurips.cc/paper/2018/hash/14491b756b3a51daac41c24863285549-Abstract.html)
6. Tony Duan et al., [NGBoost: Natural Gradient Boosting for Probabilistic Prediction](https://proceedings.mlr.press/v119/duan20a.html)
7. scikit-learn, [PCA and Factor Analysis](https://scikit-learn.org/stable/modules/decomposition.html)
